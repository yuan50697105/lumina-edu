# Lumina 墨光 · 开发进度追踪

> **WBS 依据**：`原型/lumina-launch-wbs.html` v1.1（轻量方案）
> **里程碑进度**：M1 ✅ → M2 ⏳（功能开发进行中）→ M3 → M4
> **更新**：2026-08-26

---

## 整体进度

| 阶段 | 周次 | 状态 | 说明 |
|------|------|------|------|
| 阶段一：基础环境 | W1-2 | ✅ 完成 | Docker Compose（PG/Redis/MinIO/Nginx）+ 6 运维脚本 + schemas |
| 阶段二：功能开发 | W3-6 | ⏳ 进行中 | 9/13 完成，见下 |
| 阶段三：测试优化 | W7-8 | ⬜ 待启动 | 功能测试、埋点验证、UAT |
| 阶段四：上线运营 | W9-10 | ⬜ 待启动 | 数据迁移、培训、灰度、全量 |

---

## 阶段二：功能开发（W3-6）

### ✅ 2.1 用户服务开发 + 埋点 — 完成 `d5df5c3`

| 项 | 内容 |
|----|------|
| 目录 | `服务/user-service/` |
| 功能 | 登录（学号/邮箱）、双令牌（Access 15min+Refresh 7d）、刷新、登出、改密、`/users/me` 资料查询更新、公开资料、admin 列表删除 |
| 埋点 | 全量请求日志 `api_logs`（中间件）+ 7 类业务事件：login/login_fail/register/logout/token_refresh/password_change/profile_update/user.view |
| 技术 | FastAPI 0.115 + SQLAlchemy 2.0 + JWT + bcrypt；RFC 7807 错误格式；角色权限依赖 |
| 集成 | Dockerfile（python:3.12-slim, :8080）、compose 编排、Nginx 代理 `/api/*` |
| 测试 | 单元 11 通过；集成 9 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 端点校验 + 401 拦截验证） |
| 备注 | 修复 bcrypt 4.x 与 passlib 兼容 → 锁定 `bcrypt==4.0.1` |

### ✅ 2.2 课程服务开发 + 埋点 — 完成 `25493c3`

| 项 | 内容 |
|----|------|
| 目录 | `服务/course-service/` |
| 功能 | 课程列表/详情/创建/更新（published 流转）、章节 CRUD、选课/退课（`students_count` 维护 + 唯一去重）、学生列表、公告、我的课程 |
| 埋点 | `course.*` / `chapter.*` / `announcement.*` 事件 + 全量请求日志 |
| 架构 | 跨服务认证（共享 `JWT_SECRET_KEY`）；共享库方案直接 join `users` 表取姓名 |
| 集成 | Dockerfile（:8090）、compose 扩至 6 服务、Nginx 路由 `/api/v1/courses*` → :8090 |
| 测试 | 单元 11 通过；集成 14 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 10 端点校验 + 认证拦截验证） |

### ✅ 2.3 作业服务开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/assignment-service/` |
| 功能 | 作业 CRUD/状态流转（draft/published/closed）、学生 multipart 提交（迟交自动标记）、批阅（A-F 映射、rubric 维度）、提交列表/我的提交 |
| 权限 | 学生需已选课才能提交（校验 enrollments）；教师需授课权管理/批阅 |
| 埋点 | `assignment.created/updated/submitted/graded/view` + 全量请求日志 |
| 集成 | Dockerfile（:8091）、compose 扩至 7 服务；Nginx 正则路由 `/api/v1/assignments*`、`/courses/{id}/assignments*` |
| 测试 | 单元 9 通过；集成 12 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 9 端点校验 + 越权拦截验证） |
| 备注 | 文件临时存 `uploads/`，正式环境接 MinIO；AI 批阅在 2.7 接入 |

### ✅ 2.4 成绩服务开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/grade-service/` |
| 功能 | 学期成绩录入/更新（三键唯一 upsert）、GPA 自动映射（4.0 制）、A-F 等级、我的成绩单（学分加权 GPA）、课程成绩、统计（平均/最高最低/及格率/分布） |
| 权限 | 教师需授课权（admin 豁免）；录入学生需为 student 角色 |
| 埋点 | `grade.recorded/updated/view/statistics` + 全量请求日志 |
| 集成 | Dockerfile（:8092）、compose 扩至 8 服务；Nginx 正则路由 `/api/v1/grades*`、`/courses/{id}/grades*` |
| 测试 | 单元 10 通过；集成 12 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 5 端点校验 + 越权拦截验证） |
| 备注 | `grade_records` 表 init.sql 缺失，由服务开发模式自动建表；作业级批阅在 assignment-service |

### ✅ 2.5 AI 网关服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-gateway-service/` |
| 功能 | 模型池管理（8 预置国产模型/6 供应商）、智能路由（优先级+配额）、用量记录/统计、统一协议调用 |
| 端点 | `/ai/models` 列表 · `/gateway/route` 路由 · `/gateway/completions` 统一调用 · `/gateway/calls/record` 用量 · `/gateway/usage` 统计 · 模型池/供应商 CRUD |
| 协议适配 | `adapters.py` 统一 OpenAI（国产系 `chat/completions`+Bearer）/ Anthropic（`v1/messages`+x-api-key）/ Gemini（`generateContent`+x-goog-api-key）三套协议，含 SSE 流式归一为 `{type:token/done/error}` |
| 变更 | 表结构补 `endpoint_base`（供应商 Base URL）+ `api_style`（openai/anthropic/gemini），`openai_compatible` 保留兼容；ProviderCreate/ModelCreate/ModelUpdate 均支持 endpoint_base + api_style |
| 模型配置 | **移除预置种子池**——空表启动零预置，供应商/模型由运营端管理 API 自定义（三步：注册供应商 → 注册模型 → 注入 Key）。集成测试改为 `_ensure_pool` 自建测试池，不依赖 seed |
| 埋点 | `ai.*` 事件 + 全量请求日志 |
| 集成 | Dockerfile（:8093）、compose 扩至 9 服务；Nginx `/api/v1/ai/*` → :8093 |
| 测试 | 单元 27 通过（新增 19 条 adapters 协议构造/解析/流式用例）；集成 11+ 条（需 PostgreSQL 自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 8 端点校验 + 401/403 拦截验证 + 50x 错误归一） |
| 备注 | API Key 走环境变量不入库（`.env.example`/compose 预留 qwen/glm/spark/doubao/bce/moonshot/anthropic/gemini 8 家）；未配置 Key 时 `/gateway/completions` 返回 400/502 且不泄露密钥 |

### ✅ 2.6 AI 对话服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-chat-service/`（:8094） |
| 功能 | 苏格拉底导师 SSE 流式对话、会话自动建标题/续聊、历史列表（course 过滤）/详情/删除、课程章节上下文注入 |
| 端点 | `POST /ai/chat`（SSE：token/done+conversation_id/error）· `GET /ai/conversations` · `GET/DELETE /ai/conversations/{id}` |
| 网关对接 | `gateway_client.py` 消费 `/gateway/route`（智能选模型）+ `/gateway/completions`（流式）；**用户 JWT 透传**，用量归属用户；无厂商 Key |
| 模型指定 | `X-Model` 头 / body `model_name`；缺省走网关智能路由（task_type=chat） |
| 提示词引擎 | `prompt.py` 纯函数：苏格拉底引导 + 上下文注入 + 历史窗口截断（20 条） |
| 表结构 | `ai_conversations` / `ai_messages`（对接 init.sql 与 database 文档，含 message_count/total_tokens） |
| 埋点 | `ai.chat_start/done/error` + `ai.conversation_list/view/delete` + 全量请求日志 |
| 集成 | Dockerfile（:8094）、compose 扩至 10 服务；Nginx `/ai/chat`、`/ai/conversations` → :8094（chat 开 buffering off） |
| 测试 | 单元 11 通过（提示词构造/标题生成/历史截断/Schema 校验）；集成 5 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 3 端点 + 401 拦截 + 无模型 400 错误路径验证） |
| 备注 | 无模型池时对话返回 400「暂无可用模型」；流式链路 前端→nginx→ai-chat→ai-gateway→厂商，含错误归一，await 2.12 联调 |

### ✅ 2.7 AI 批阅服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-grade-service/`（:8095） |
| 功能 | `POST /ai/grade` LLM 辅助批阅：校验作业/提交 → 智能路由(task_type=grade) → 模型按 rubric JSON 输出 → 容错解析 → 写 grades 表 → 结构化成绩 |
| 评分标准 | 请求 rubric（维度/权重/满分），缺省复用作业 rubric；模型可 `X-Model`/model_name 指定 |
| 容错解析 | `extract_json` 兼容围栏/前导文字/尾注；`parse_grade_result` 防御缺字段、分数 clamp [0,max]、置信度 clamp [0,1]、坏条目跳过 |
| 落库 | `grades` 按 submission_id 唯一 upsert；`graded_by='ai'` + grader_id + ai_model + confidence + rubric_scores |
| 埋点 | `ai.grade_start/done/error`（置信度/总分/维度数/token/延迟） + 全量请求日志 |
| 集成 | Dockerfile（:8095）、compose 扩至 11 服务；Nginx `/ai/grade` → :8095 |
| 测试 | 单元 19 通过（提示词/Rubric 文本/JSON 容错解析/Schema 校验）；集成 4 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 端点 + 401/403/404/400/502 错误路径验证） |
| 备注 | 批阅质量依赖模型 JSON 稳定性；失败归为 502 提示重试不落脏数据；`/assignments/{id}/grade` 触发 AI 批阅在 2.12 联调接入 |

### ✅ 2.8 前端 Web 开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/web-frontend/`（React 18.3 + TS 5.6 + Vite 5 + React Router 6 + Zustand） |
| 页面 | 登录 / 首页（我的课程+广场）/ 课程详情（章节+选课退课）/ AI 导师（SSE 流式对话+历史）/ 成绩单 / 模型池配置（运营端） |
| API 层 | `src/api/client.ts` fetch 封装：BASE=/api/v1、JWT 注入、401 登出、错误归一（RFC7807 detail 解析） |
| 埋点 SDK | `src/utils/tracker.ts`：`page.view`（路由变化）+ `element.click`（全局捕获 data-track）+ 业务事件（auth.login/ai.chat.send…）；sendBeacon→fetch keepalive→localStorage 队列重试；事件 POST `/api/v1/events`（对齐 event_tracking 表，2.10 收集） |
| AI 对话 | SSE 解析（ReadableStream + TextDecoder 逐行 data: 事件）增量渲染、新会话/续聊、错误兜底展示 |
| 模型池 UI | 供应商/模型列表、注册模型表单（供应商+api_style 协议+任务类型 chips）、启停 toggle —— 呼应「运营端自定义模型」 |
| 构建 | Vite proxy `/api → :8080`；`npm run build`（tsc -b + vite）通过；多阶段 Dockerfile（node→nginx） |
| 测试 | tsc 严格模式类型检查通过（build 成功 58 modules） |
| 审核 | ✅ Claude 团队审核通过（构建产物 dist/ 验证 + 埋点链路设计对齐后端表结构） |
| 备注 | 埋点端点 `/api/v1/events` 由 2.10 提供，当前 fire-and-forget 不阻塞业务；`/ai/conversations`、`/grades/me` 等联调在 2.12 |

### ✅ 2.10 埋点数据收集与存储 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/analytics-service/`（:8096） |
| 功能 | 事件收集：单条 `POST /events`(202) + 批量 `POST /events/batch`(≤100/事务)；统计：`GET /events/stats` 概览 + `GET /events/breakdown` 事件类型分布（admin） |
| 字段对齐 | `event_tracking`：event_name/user_id/session_id/course_id/properties/page_url + 服务端补 user_agent/ip(x-forwarded-for 首段)；`properties.course_id` 合法 UUID 自动提升为列 |
| 身份模型 | 有合法 Access Token → JWT sub 权威覆盖（防伪造）；游客信任客户端 UUID；非法忽略 |
| 埋点闭环 | 前端 SDK（sendBeacon→fetch keepalive→localStorage 队列）→ Nginx `/api/v1/events`（event_limit 独立 zone 600r/m）→ 本服务 → event_tracking 表；离线重放走 /batch |
| 集成 | Dockerfile（:8096）、compose 扩至 12 服务；Nginx `/api/v1/events` → :8096 |
| 测试 | 单元 14 通过（_cell_uuid/_resolve_user token 覆盖/过期回退/course_id 提升/非法保留/Schema 边界/batch 上限）；集成 5 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 4 端点 + 游客 202 + stats 401/403 验证） |
| 备注 | 各业务服务 `Instrumentation.track` 直写 DB 不经本服务（旧路径兼容）；前端 SDK 事件走统一收集端点，为跨端埋点归口 |

### ⬜ 待完成任务

| 编号 | 任务 | 状态 |
|------|------|------|
| 2.9 | 前端移动端开发 + 埋点 | ⬜ 未开始 |
| 2.11 | 基础日志系统 | ⬜ 未开始 |
| 2.12 | API 联调与测试 | ⬜ 未开始 |
| 2.13 | 单元测试编写 | ⬜ 未开始 |

---

## 里程碑

| 里程碑 | 定义 | 状态 |
|--------|------|------|
| M1 | 基础环境可用（compose 一键启动 4 基础服务） | ✅ W2 达成 |
| M2 | 核心服务开发完成（用户/课程通过联调） | ⏳ 进行中 |
| M3 | 测试通过（全部用例 + 埋点验证） | ⬜ W8 |
| M4 | 正式上线（数据迁移 + 灰度 + 全量） | ⬜ W10 |

---

## 开发环境备注

- 本机仅 Python 3.14.4 → 本地验证用 `.venv` 新版本包；生产锁定版本跑在 Docker python:3.12
- PostgreSQL 未起时，集成测试自动跳过（skip），不阻塞 CI
- Windows GBK 编码：运行 `pytest` 前设置 `PYTHONIOENCODING=utf-8`