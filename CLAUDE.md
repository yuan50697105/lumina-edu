# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Lumina (墨光) 教育应用 UI 设计系统 - 面向高校师生的跨端教学协作平台。本项目为设计原型 + 技术文档 + FastAPI 单体应用（`服务/lumina-app/`）。单体快照：171 paths / 212 ops · 19 模块 · 62 表（模块：user / course / assignment / grade / live / collab / notif / exam / ai_gateway / ai_chat / ai_grade / analytics / logs / tutoring / admin / settings / learning / video / ai_infra）。

## Directory Structure

```
edu/
├── 原型/                    # 设计原型 + 技术文档
│   ├── lumina-00-index.html    # 索引导航页（入口）
│   ├── lumina-prd.html         # 产品需求文档 v1.3
│   ├── lumina-tdd.html         # 技术设计文档 v1.0
│   ├── lumina-api.html         # API 接口文档 v1.0
│   ├── lumina-api-openapi.yaml # OpenAPI 3.1 规范
│   ├── lumina-database.html    # 数据库设计文档 v1.0
│   ├── lumina-operations.html  # 部署运维手册 v1.0
│   ├── lumina-testcases.html   # 测试用例文档 v1.0
│   ├── lumina-userguide.html   # 用户手册 v1.0
│   ├── lumina-launch-wbs.html  # 上线 WBS v1.1
│   ├── lumina-learning-system.html  # D-06 自主学习与闯关奖励需求 v1.0
│   ├── lumina-status-report.html    # 项目全景状态报告 v1.0（里程碑/模块/技术栈/质量/风险）
│   │
│   ├── APP/phone/  (9 files)   # 手机 APP 原型
│   ├── APP/tablet/ (6 files)   # 平板 APP 原型
│   ├── Web/        (9 files)   # 浏览器端原型
│   ├── PC/         (7 files)   # 桌面端原型
│   ├── AI/         (3 files)   # AI 模块原型
│   └── Harmony/    (1 file)    # 📟 鸿蒙生态原型（D-11 六设备形态）
│
├── 服务/                   # 单体应用 + 前端
│   ├── lumina-app/             # ✅ 单体 FastAPI（19 模块 · 171 paths · 62 表）
│   │   ├── app/                # 应用代码（main.py / models.py / security.py ...）
│   │   │   └── modules/        # 14 业务模块：user/course/assignment/grade/live/collab/notif/exam/ai_gateway/ai_chat/ai_grade/analytics/logs/tutoring
│   │   ├── tests/              # 单元测试（215 passed）
│   │   ├── requirements.txt    # Python 依赖
│   │   └── Dockerfile          # API 容器镜像
│   ├── web-frontend/           # Web 前端（React 18 + TS + Vite）
│   └── mobile-app/             # 移动端前端（React Native / Expo）
│
├── 部署/                   # Docker 部署方案（环境搭建）
│   ├── docker-compose.yml      # 服务编排（MySQL/Redis/lumina-app/Nginx）
│   ├── .env.example            # 环境变量模板
│   ├── config/                 # Nginx/MySQL 配置
│   ├── scripts/                # 启停/备份/监控/契约核对脚本
│   ├── docs/                   # UAT 验收清单 / events-catalog / 上线检查清单
│   └── README.md               # 部署使用说明
│
└── scripts/                 # 工具脚本
    ├── yuque-sync.py           # 语雀同步工具
    └── README.md               # 脚本使用说明
```

## Common Commands

### Viewing Prototypes
All prototype files are standalone HTML. Open directly in browser:
```bash
# Open index page
open 原型/lumina-00-index.html

# Or use any browser
chrome 原型/lumina-prd.html
```

### Running the Monolith Application (单体应用)
单体应用为 FastAPI，位于 `服务/lumina-app/`，启动命令：
```bash
# 进入单体应用目录
cd 服务/lumina-app

# 启动 API 服务（默认端口 8080）
uvicorn app.main:app --port 8080
```

### Running Unit Tests (单元测试)
```bash
PYTHONIOENCODING=utf-8 服务/lumina-app/.venv/Scripts/python.exe -m pytest 服务/lumina-app/tests -q
```

### Yuque Sync (语雀同步)
```bash
# Install dependencies
pip install requests beautifulsoup4 markdownify

# Set token
export YUQUE_TOKEN="your_token"

# Run sync
python scripts/yuque-sync.py
```

### Test Accounts（测试账号 / Demo）

本地联调、冒烟与前端开发使用的演示账号（由 `部署/scripts/seed_demo.py` 种子生成，幂等可重复运行；默认密码 `Demo@2026`，可用 `--demo-password xxxx` 覆盖）：

| 角色 | 账号 | 默认密码 | 说明 |
|------|------|----------|------|
| 管理员 | `admin@lumina.edu` | `Demo@2026` | 系统管理员：模型池管理、绕过课程权限控制 |
| 教师 | `teacher@lumina.edu` | `Demo@2026` | 授课教师（工号 `T20260001`）：建课程/直播、点名、发答题、批阅 |
| 学生 | `student@lumina.edu` | `Demo@2026` | 已选课学生（学号 `20260001`）：加入直播、举手、作答 |
| 未选课学生 | `nouser@lumina.edu` | `Demo@2026` | **未选任何课程**，用于越权测试（直播加入 403、课程权限校验） |

> ⚠️ 仅演示/联调使用，生产环境必须更换。上层应用连本机 MySQL 时用 `root/root`（见环境变量说明）。

运行前端体验直播：
```bash
# 后端（需先起 MySQL）
cd 服务/lumina-app && uvicorn app.main:app --port 8080 --host 127.0.0.1
# 前端 dev（已配置 5173 → 127.0.0.1:8080 代理）
cd 服务/web-frontend && npm run dev   # 打开 http://localhost:5173
```

### 🎥 直播演示流（开播真实画面，可选）

未接媒体服务器时，开播返回 `mock://` 占位（无画面，仅课堂协作逻辑）。要「开播即有真实画面」，用本地 mediamtx 演示：

```bash
# ① 一键起流：MediaMTX + ffmpeg 推演示课堂画面（默认房间 key=roomdemo）
部署/stream/start_demo_stream.bat          # 停止：stop_demo_stream.bat
# ② 后端 .env 已配置（服务/lumina-app/.env，gitignored）：
#    LIVE_STREAM_BASE=http://127.0.0.1:8888/live
#    LIVE_STREAM_PROXY=true        # stream_url 走同源 /media 反代
# ③ 教师登录 → 冒烟测试课程 → 「直播演示间（固定流 roomdemo）」→ 开播
```
- 任意房间推流：`部署/stream/start_demo_stream.bat <room的 stream_key>`
- 详情见 `部署/stream/README.md`；生产接入见「运维手册」媒体反代说明

### 📱 移动端直播演示（Expo）

移动端（`服务/mobile-app/`，RN/Expo）直播页功能对齐 Web。本地连接后端：

```bash
# ① 后端（需先起 MySQL）；若手机真机访问，--host 0.0.0.0
cd 服务/lumina-app && uvicorn app.main:app --port 8080 --host 0.0.0.0
# ② 移动端 API 地址（电脑上回环仅模拟器可用）：
#    iOS 模拟器 → http://127.0.0.1:8080（默认）
#    Android 模拟器 → http://10.0.2.2:8080
#    真机 → http://<电脑局域网IP>:8080
#    修改：mobile-app/src/config.ts 的 API_BASE
cd 服务/mobile-app && npm install && npx expo start
```

- 开播真实画面：先起 `部署/stream/start_demo_stream.bat`（HLS 同源 `/media` 反代已配），再进「直播演示间」开播
- HLS 播放：`expo-video`（原生播放器）；仅房间 `status==='live'` 时拉流，`mock://` 为未接媒体服务器占位

### 🆕 平台支持矩阵（含鸿蒙）

| 端 | 现状 | 说明 |
|----|------|------|
| iOS / Android | ✅ 已实现 | `服务/mobile-app/`（React Native / Expo SDK 52） |
| Web | ✅ 已实现 | `服务/web-frontend/`（React 18 + Vite） |
| 桌面 | 🟡 原型 | `原型/PC/`（Electron 方案规划中，TDD §3.2） |
| 鸿蒙 HarmonyOS | ⏳ 演进评估 | **Expo 官方不支持鸿蒙**；WBS D-09 三路线评估：原生 ArkTS／ArkUI 独立客户端（DevEco，移动端 6 页 · 推荐）· RNOH bare 迁移 · uni-app；不阻塞 M4 |

> 详见 `原型/lumina-wbs-pending.html` v1.1 D-09 任务包。

## Design System

### Color Palette
- **宣纸色**: `#FAF6EC` (主背景)
- **墨色**: `#0F1020` (主文字)
- **钴蓝**: `#3D46C9` (品牌强调色)
- **荧光黄**: `#F5B800` (签名效果)
- **警示红**: `#E85D3A` (批注/警示)
- **成长绿**: `#2A7F4F` (成功/正向)
- **AI 紫**: `#7C3AED` (AI 模块强调)

### Typography
- **Fraunces** - Display 标题字体
- **Inter** - Body 正文字体
- **JetBrains Mono** - Mono 数字/代码字体
- **PingFang SC / Songti SC** - 中文 fallback

### Signature Effect (荧光笔签名)
```css
background: linear-gradient(
  180deg,
  transparent 62%,
  var(--highlighter) 62%,
  var(--highlighter) 88%,
  transparent 92%
);
```

## Documentation Architecture

项目包含 9 个核心文档，形成完整文档体系：

1. **PRD** (产品需求) - 用户画像、功能模块、验收标准
2. **TDD** (技术设计) - 架构、单体应用（19 模块）、数据库、API 规范
3. **API** (接口文档) - 122 paths / 159 ops
4. **OpenAPI** (机器可读) - YAML 格式 API 规范
5. **Database** (数据库) - 62 表、ER 模型、分区策略
6. **Operations** (运维) - Docker Compose 部署、监控告警、Runbook
7. **Test Cases** (测试) - 215 用例、80% 覆盖率
8. **User Guide** (用户手册) - 3 角色指南、FAQ
9. **WBS v1.1** (上线计划) - 10 周轻量方案

所有文档使用统一的 Lumina 视觉风格，可直接在浏览器中打开查看。

单体应用 `服务/lumina-app/` 快照：171 paths · 212 ops · 19 模块 · 62 表 · 单元测试 321 通过。

## Platform Coverage

| 平台 | 目录 | 文件数 | 说明 |
|------|------|--------|------|
| 📱 手机 | APP/phone/ | 9 | 学生/教师移动端 + D-06/D-08 学习视频 |
| 📲 平板 | APP/tablet/ | 6 | 大屏触控优化 |
| 🌐 Web | Web/ | 9 | 浏览器端 + D-06 学习系统 + D-08 视频系统 |
| 🖥️ 桌面 | PC/ | 7 | 桌面客户端 |
| 🤖 AI | AI/ | 3 | AI 功能模块 |
| 📟 鸿蒙 | Harmony/ | 1 | D-11 鸿蒙六设备形态（手机/平板/PC/智慧屏/穿戴/车机）|

**总计**: 49 个 HTML 文件（35 接口原型 + 14 根目录文档）

## Git Workflow

- 主分支: `master`
- 远程仓库: https://github.com/yuan50697105/lumina-edu
- 提交规范: `docs: 描述变更内容`
- 所有变更及时推送，保持同步

## Key Conventions

1. **文件命名**: `lumina-{编号}-{功能}.html` (如 `lumina-03-student-mobile.html`)
2. **文档版本**: 每个文档独立版本号 (v1.0, v1.1 等)
3. **视觉一致**: 所有 HTML 文档使用相同的 CSS 变量和样式
4. **中文优先**: 所有文档内容使用简体中文
5. **HTML 格式**: 原型和文档均为纯 HTML，无外部依赖（字体除外）

## 🏃 Agile Development (敏捷开发 · 战略节奏层)

> **两层协作模型**：本章节管「做什么 × 何时做 × 节奏如何」（战略节奏层）；下方 §⚠️ Mandatory Workflows 管「单次任务怎么做、边界在哪」（战术边界层）。两层互补，不互相替代——每个任务既要符合敏捷节奏，也要严守工作流边界。

### 🎯 核心原则（敏捷宣言 Lumina 适配版）

1. **可工作的软件 > 详尽文档**（但本项目为「软件 + 文档」双产物，二者同等重要）
2. **响应变化 > 遵循计划**（但变更必须走工作流 5，不可绕过）
3. **个人与交互 > 流程与工具**（流程服务于产出，不要反客为主）
4. **可持续节奏 > 冲刺透支**（不熬夜、不堆积压，保持可维护的开发速度）
5. **小步快跑 > 大爆炸交付**（每个 WBS 任务包 ≤ 5 个工作日，拆不动就先做方案分解走规划工作流）
6. **需求不明确时，先问再做**（不要脑补，宁可多一轮确认）

### 📅 Sprint 节奏（与 WBS 对齐）

本项目为**个人 + Claude 协作**模式，采用轻量级 Scrum 适配：

| 实践 | 周期 | 内容 | 产出物 |
|------|------|------|--------|
| **Sprint 规划** | 每 1-2 周（或每个 WBS 任务包） | 从 `原型/lumina-wbs-pending.html` 拉取 3-5 个任务进入冲刺 | 冲刺 Backlog（写入 `开发进度.md` 的「当前冲刺」段） |
| **每日自站会** | 每次开工第一句话 | 三句话：昨天做了什么 / 今天做什么 / 有什么阻塞 | 无需记录，直接体现在 commit 里 |
| **Sprint 评审** | 每 Sprint 结束 | 演示本轮产出、对照 DoD 验收 | `开发进度.md` 更新状态 |
| **Sprint 回顾** | 每 Sprint 结束 | 什么做得好 / 什么改进 / 下个 Sprint 调整 | 口头自述，重大改进写入 `开发进度.md` |

**Sprint 容量规则**：
- 单个 Sprint 不超过 5 个任务单元（一个原型 = 1，一个模块 = 2-3，一组文档 = 1-2）
- 每个 Sprint 必须留有 **20% 缓冲** 应对临时需求或 Bug
- 容量溢出 → 拆分到下一 Sprint，**不可压缩质量**

### 📝 User Story 格式（任务原子单元）

每个待办任务都应写成用户故事，格式：

```
作为 <角色>，我希望 <功能>，以便 <价值>。
验收标准（AC）：
- [ ] AC1: 具体可验证的条件
- [ ] AC2: 具体可验证的条件
- [ ] AC3: 通过工作流门禁
```

**故事类型与验收标准基线**：

| 故事类型 | 角色示例 | DoD（Definition of Done）强制项 | 对应工作流 |
|---------|---------|-------------------------------|-----------|
| 🖼️ 原型故事 | 学生 / 教师 / 管理员 | ① HTML 在浏览器正常渲染 ② 主要交互可点击 ③ 使用 Lumina 设计系统变量 ④ 索引页已同步 | 📋 工作流 1 |
| 🔌 接口故事 | 前端 / 第三方系统 | ① 路由实现完成 ② ≥1 单测通过 ③ Pydantic schema 与 OpenAPI YAML 一致 ④ `开发进度.md` 已更新 | 🔨 工作流 3 |
| 📄 文档故事 | 所有干系人 | ① 版本号已升级 ② 章节内容准确 ③ 关联文档已联动（如 API 改 → OpenAPI 同步） | 🔄 工作流 5 |
| 🏗️ 重构故事 | 开发维护 | ① 无回归（全量测试通过） ② 性能不降级 ③ 调用方已通知/文档已更新 | 🔄 工作流 5 或 📐 工作流 2 |

**故事拆分原则（INVEST）**：
- **I**ndependent - 独立，不依赖其他未完成任务
- **N**egotiable - 可协商，具体实现细节可调整
- **V**aluable - 有价值，完成即可交付（哪怕不完美）
- **E**stimable - 可估算，≤ 5 天能完成
- **S**mall - 足够小，拆分到可独立验收
- **T**estable - 可测试，有明确 DoD

### ✅ Definition of Done（通用 DoD）

**每个"完成"的任务必须同时满足以下全部项，缺一不可**：

- [ ] 代码 / 文档已 `git commit` 并 `git push origin master`
- [ ] 单测通过（代码类）或 浏览器渲染正常（文档类）
- [ ] `开发进度.md` 状态已更新（任务状态 + 提交 ID + 验证结果）
- [ ] 索引页 / README 已同步（如本次新增文件）
- [ ] 工作流边界检查通过（无越界产出，对照白名单）
- [ ] 团队审核通过（重大变更需显式审核，小变更自审通过）
- [ ] **未引入新 regression**（全量测试 `pytest -q` 通过，文档无死链）

**DoD 升级机制**：每个里程碑（M1 / M2 / M3 / M4）结束时回顾 DoD，根据当前痛点追加检查项，不降级。

### 📋 看板管理（任务可视化）

使用 `开发进度.md` 作为**单一看板**，每个任务必须有状态标记：

```
| 状态 | 含义 | 允许流转 |
|------|------|---------|
| 📋 Backlog | 待进入冲刺 | → 🏃 In Progress |
| 🏃 In Progress | 当前冲刺进行中 | → ✅ Done / 📋 Backlog（移出） |
| ✅ Done | 已完成且符合 DoD | 终态 |
| 🚫 Blocked | 被阻塞，无法继续 | → 🏃 In Progress（阻塞解除后） |
```

**看板规则**：
1. **WIP 限制**：同一时刻 `In Progress` 任务 ≤ 2 个（避免上下文切换）
2. **显式推进**：状态变更必须伴随 commit 或 `开发进度.md` 更新
3. **先完成再开始**：一个任务达到 Done 后，才能拉取下一个
4. **阻塞即上报**：Blocked 超过 1 天必须记录原因并寻求解决方案

**任务条目格式**（写入 `开发进度.md`）：
```
- [ ] D-05.1 学情分析接口 | 2026-xx-xx | 提交 ID: abc123 | ✅ 单测通过
- [x] D-05.2 学生档案模块 | 2026-xx-xx | 提交 ID: def456 | ✅ 单测通过 | 审核通过
```

### 📊 速度跟踪（Velocity）

- **速度指标**：每 Sprint 完成的"任务单元数"
- **记录位置**：`开发进度.md` 每 Sprint 结束时追加速度总结
- **用途**：下个 Sprint 容量规划的依据，不用于考核
- **健康速度**：稳定 > 冲高；连续 2 个 Sprint 速度下降 → 回顾调整

**速度报告模板**（追加到 `开发进度.md`）：
```
## Sprint N 总结（YYYY-MM-DD ~ YYYY-MM-DD）
- 计划任务数：X
- 完成任务数：Y
- 速度：Y 单元
- 遗留任务：Z（移入下一 Sprint Backlog）
- 回顾：[做得好] / [改进点]
```

### 🔄 Ceremonies（轻量仪式）

| 仪式 | 频率 | 时长 | 参与者 | 产出 |
|------|------|------|--------|------|
| 开工自站会 | 每次会话开始 | 1 分钟 | 人 + Claude | 本次任务声明 + 工作流选择 |
| 阻塞排查 | 遇到即触发 | 不限 | 人 + Claude | 阻塞原因 + 解决方案记录 |
| 冲刺评审 | 每 Sprint 结束 | 15 分钟 | 人 + Claude | DoD 验收 + 进度更新 |
| 冲刺回顾 | 每 Sprint 结束 | 10 分钟 | 人 + Claude | 改进项（写入进度） |
| 里程碑复盘 | M1/M2/M3/M4 结束 | 30 分钟 | 人 + Claude | 阶段总结 + DoD 升级 |

### 🎯 与强制工作流的协作关系

| 敏捷环节 | 触发时机 | 必须调用的工作流 |
|---------|---------|----------------|
| Sprint 规划会选定任务 | 每 Sprint 开始 | 任务如为新文档 → 📋 工作流 1；新代码 → 🔨 工作流 3；改动 → 🔄 工作流 5 |
| 用户故事拆分 | 任务进入 Backlog | 架构不清 → 📐 工作流 2 先做方案 |
| 故事执行 | 每个任务实际动手 | 严格对照 §⚠️ 工作流选择矩阵，**先声明后执行** |
| DoD 验收 | 故事标记 Done 前 | ✅ 工作流 4（收尾验证）承担 DoD 中的"验证"部分 |
| 回顾与调整 | Sprint 结束 | 流程变更 → 🔄 工作流 5 修改本 CLAUDE.md |

### 📌 滚动式规划（Rolling Wave）

本项目 WBS 采用滚动式规划，不预先细化所有任务：

```
M1 基座       ████████████  (已细化到任务包)
M2 协作       ████████████  (已细化到任务包)
M3 智能       ████████████  (已细化到任务包)
M4 上线       ████░░░░░░░░  (里程碑级，临近再细化)
D-06 管理端   ████████░░░░  (并行推进，按周滚动)
D-07 AI 基建  ████░░░░░░░░  (方案先行，开发跟进)
D-08 数据中台 ████░░░░░░░░  (待前置 M2 完成)
D-09 多端鸿蒙 ████░░░░░░░░  (演进评估，不阻塞 M4)
```

**规划颗粒度规则**：
- 当前 Sprint：任务级（含验收标准 AC）
- 下一个 Sprint：任务包级（WBS x.x 粒度）
- 更远 Sprint：里程碑级（D-0x / Mx 粒度）
- 临近再细化：避免过早投入设计成本

### 🚨 敏捷反模式（本项目禁止）

| 反模式 | 为什么有害 | 正确做法 |
|--------|----------|---------|
| 「英雄冲刺」熬夜赶工 | 透支健康 + 引入缺陷 | 保持节奏，容量溢出移入下一 Sprint |
| 「流程拜物教」仪式重于产出 | 仪式服务于交付，不是反过来 | 仪式可简化，不可省略核心价值（DoD、回顾） |
| 「需求脑补」不确认就开工 | 做错比不做成本更高 | 不明确 → 先问用户，写入 AC 再动手 |
| 「绕流程加急」临时需求跳过工作流 | 破坏边界约束，越界风险高 | 再急也必须先声明走哪个工作流 |
| 「僵尸任务」长期 In Progress | 掩盖真实进度 | In Progress > 3 天必须拆分或标记 Blocked |
| 「无记录完成」完成了但没更新进度 | 没有记录 = 没有发生 | DoD 要求 `开发进度.md` 必须同步 |
| 「大爆炸交付」攒一堆一次性推送 | 难追溯、难回滚 | 增量提交，小步推送 |

---

## ⚠️ Mandatory Workflows (强制工作流 · 严格边界版)

> **核心原则**：一个任务 = 一个工作流 = 一条边界。禁止混合、禁止越界、禁止事后补选。

### 🛑 全局门禁（开工前必读）

**每次收到任务，必须先执行以下三步，禁止跳过：**

1. **判定任务类型** → 对照下方「工作流选择矩阵」锁定唯一工作流
2. **声明边界** → 在首次回复中明确写出「本次走【工作流 X】，边界为：…」
3. **自检越界** → 每个产出物对照该工作流的「反向禁止清单」

**越界判定标准（任何一条触发即视为违规，必须停工重选工作流）**：
- ❌ 在需求/规划工作流中写了业务代码（`.py` / `.ts` / `.tsx` 新增或修改逻辑）
- ❌ 在开发工作流中创建了新的 PRD/TDD/WBS/原型 HTML
- ❌ 在变更工作流中新增文件（新增 = 需求工作流，不是变更）
- ❌ 在收尾验证工作流中修复 Bug 或新增功能（验证发现缺陷 → 回到开发/变更工作流，不在验证流里修）
- ❌ 未经选择直接开工，或"边走边看"
- ❌ 同一任务同时走两个工作流（拆任务再分别走）

---

### 📊 工作流选择矩阵（门禁表 · 开工前唯一依据）

> **顺序**：先查此表 → 锁定工作流 → 声明边界 → 再开工。此表优先级高于任何经验判断。

| 任务特征（任一命中） | 锁定工作流 | **反向禁止**（不可做的事） |
|---------|-----------|----------|
| 新建文档 / 新建原型 / 首次定义需求 | 📋 工作流 1：需求 | 🚫 禁写业务代码；🚫 禁做架构决策（那是规划流） |
| 架构设计 / 技术方案 / WBS 分解 / 选型 | 📐 工作流 2：规划 | 🚫 禁写业务代码；🚫 禁直接产出最终 HTML（那是需求流） |
| 写 `.py` / `.ts` / `.tsx` 业务代码、脚本 | 🔨 工作流 3：开发 | 🚫 禁新建 PRD/TDD/原型；🚫 禁做架构变更（那是规划流） |
| 写单测 / 集成测试 / 性能测试代码 | 🔨 工作流 3：开发 | 🚫 同上 |
| 修改已存在的文档/原型/代码/配置 | 🔄 工作流 5：变更 | 🚫 禁新增文件（新增走需求流）；🚫 禁重构架构（走规划流） |
| 修复 Bug（改已有代码） | 🔄 工作流 5：变更 | 🚫 禁借机新增功能（新增功能走开发流） |
| 阶段验收 / 上线前检查 / 交付确认 | ✅ 工作流 4：收尾验证 | 🚫 禁修 Bug；🚫 禁加功能；发现问题 → 转交工作流 3/5 |

**边界模糊时的仲裁规则**：
- 「新建 + 修改」混合任务 → **拆分为两个子任务**，分别走需求流 + 变更流
- 「开发 + 架构调整」混合 → **拆分**，先规划流锁定方案，再开发流实现
- 「验证发现 Bug」 → **不继续验证流**，转开发/变更工作流修复后，**重新进入验证流**

---

### 📋 工作流 1：需求工作流 (Requirement Workflow)

**边界定义**：只产出**新的** HTML 文档 / 原型。只处理"从 0 到 1"的需求定义。
**反向禁止清单**：
- 🚫 不得修改 `.py` / `.ts` / `.tsx` 等业务代码（哪怕只是"配套调整"）
- 🚫 不得做技术方案决策（如数据库选型、模块拆分）——那是规划工作流
- 🚫 不得修改已有文档——那是变更工作流（工作流 5）

**步骤**：

| 步骤 | 操作 | 检查点 |
|------|------|--------|
| 1. 需求分析 | 明确文档/原型的目标、范围、用户 | ✅ 需求清晰 |
| **1.5 合规预检** | **识别本需求涉及的法律合规领域（见下方"教育平台适用法律清单"），标记需合规审查的 AC** | **⚖️ 合规域已识别** |
| **1.6 测试需求分析** | **对每条 AC 标注测试方法（自动化/手工/视觉回归），识别不可测试需求并拆分** | **🧪 AC 均可测试** |
| 2. 结构设计 | 设计章节结构、内容大纲、交互流程 | ✅ 结构合理 |
| **2.5 原型设计** | **创建可交互原型（HTML），覆盖核心用户流程与关键页面，标注交互说明** | **🎨 原型已完成** |
| **2.6 原型评审** | **团队评审原型（交互、视觉、可用性），收集反馈并迭代** | **✅ 原型已通过** |
| 3. 创建文档 | 使用 Lumina 模板创建 HTML，填充内容（含原型截图与说明） | ✅ 视觉一致 |
| 4. 更新索引 | 更新索引页 + README + 统计数据 | ✅ 索引同步 |
| 5. 验证提交 | 浏览器检查，提交推送 | ✅ 功能正常 |
| **6. 团队审核** | **审核团队评审（含合规审查 + 测试可行性审查 + 原型验收），确认通过** | **✅ 审核通过** |

**⚖️ 法律合规约束（需求阶段）**

> **强制规则**：任何涉及个人信息、未成年人、AI 算法、教育内容的需求，**必须在验收标准（AC）中显式列出对应合规要求**，否则视为需求不完整，不得进入下一步。

**合规预检清单**（每新增一个需求文档/原型，逐条勾选）：

- [ ] **个人信息处理**：是否涉及收集个人信息（姓名、手机号、学号、人脸、成绩等）？
  - ✅ 是 → 必须遵循《个人信息保护法》：明示同意、目的限制、最小必要、存储期限
  - 📝 在 AC 中追加：`ACx: 隐私政策已更新，用户已明示同意`
- [ ] **未成年人保护**：是否涉及 18 岁以下用户（学生端主体）？
  - ✅ 是 → 必须遵循《未成年人保护法》《未成年人网络保护条例》：监护人同意、防沉迷、内容分级
  - 📝 在 AC 中追加：`ACx: 未成年人模式已启用 / 监护人同意流程已实现`
- [ ] **AI 生成内容**：是否使用 AI 生成文本/图像/评分（`ai_gateway` / `ai_chat` / `ai_grade` 模块）？
  - ✅ 是 → 必须遵循《生成式人工智能服务管理暂行办法》：AI 标识、内容审核、训练数据来源披露
  - 📝 在 AC 中追加：`ACx: AI 生成内容已打标识 / 内容安全审核已接入`
- [ ] **算法推荐**：是否使用个性化推荐（学习路径、题目推荐、智能分组）？
  - ✅ 是 → 必须遵循《互联网信息服务算法推荐管理规定》：算法备案、用户可关闭推荐
  - 📝 在 AC 中追加：`ACx: 算法推荐已备案 / 用户可关闭个性化推荐`
- [ ] **教育内容版权**：是否涉及教材、课件、题库等受版权保护的内容？
  - ✅ 是 → 必须遵循《著作权法》：授权链条、合理使用边界、侵权投诉机制
  - 📝 在 AC 中追加：`ACx: 内容来源授权已核实 / 侵权投诉通道已建立`
- [ ] **数据跨境**：是否涉及向境外传输数据（境外云服务、海外用户）？
  - ✅ 是 → 必须遵循《数据安全法》《数据出境安全评估办法》：安全评估、标准合同
  - 📝 在 AC 中追加：`ACx: 数据出境评估已完成`
- [ ] **电子认证/签名**：是否涉及电子证书、学分证明、成绩单等具有法律效力的文书？
  - ✅ 是 → 必须遵循《电子签名法》：可靠电子签名、CA 认证
  - 📝 在 AC 中追加：`ACx: 电子文书已采用可靠电子签名`

**未命中任何合规域的需求**：在 PRD 中标注 `⚖️ 合规域：无`，仍可继续推进。  
**命中任一合规域但未完成 AC 追加**：需求视为**不完整**，必须返回步骤 1.5 补齐。

**🧪 测试需求分析（步骤 1.6）**

> **强制规则**：每条 AC 都必须标注测试方法，**不可测试的 AC 视为需求不合格**，必须返回步骤 1 拆分或重写。

**AC 测试方法标注格式**：

```markdown
- [ ] AC1: 用户登录成功后跳转到首页
  - 测试方法：🤖 E2E 自动化（Playwright）
  - 断言：URL === '/home' && 首页元素可见
- [ ] AC2: 页面视觉符合 Lumina 设计系统
  - 测试方法：👁️ 视觉回归（Chromatic / Playwright screenshot）
  - 基线：原型 lumina-xx.html 的截图
- [ ] AC3: 复杂交互（拖拽排序、手写批注）体验流畅
  - 测试方法：🖐️ 手工测试 + 录屏
  - 验收：≥3 人手工验证通过
```

**测试方法分类**：

| 标记 | 类型 | 适用场景 | 工具 |
|------|------|---------|------|
| 🤖 E2E | 端到端自动化 | 可脚本化的用户流程 | Playwright / Cypress |
| 🧩 API | 接口自动化 | 后端路由、数据契约 | pytest + httpx |
| 👁️ VR | 视觉回归 | UI 像素级对齐 | Chromatic / Percy / Playwright screenshot |
| 🖐️ Manual | 手工测试 | 复杂交互、主观体验 | 人工 + 录屏 |
| 📊 Perf | 性能测试 | 并发、响应时间 | Locust / k6 |
| 🔒 Sec | 安全测试 | 越权、注入、CSRF | OWASP ZAP / 手工 |
| ⚖️ Comp | 合规测试 | 隐私、未成年人、AI 标识 | 手工 + 自动化混合 |

**测试需求分析输出**：
- ✅ **需求-测试追踪矩阵（RTM）**：每条 AC 都映射到至少一种测试方法
- ✅ **测试优先级标注**：P0（阻塞发布）/ P1（必须）/ P2（应该）/ P3（可选）
- ✅ **测试数据需求**：识别需要的测试账号、测试数据、Mock 数据
- ❌ **不可测试的 AC**：返回步骤 1 重写（如"用户体验好" → 改为"3 名测试用户完成核心流程无阻塞"）

**RTM 模板**（附加在需求文档末尾）：

| AC 编号 | AC 描述 | 测试方法 | 测试优先级 | 测试数据 | 责任方 |
|---------|---------|---------|-----------|---------|--------|
| AC1 | 用户登录成功跳转首页 | 🤖 E2E | P0 | teacher@lumina.edu | 开发 |
| AC2 | 页面视觉符合设计系统 | 👁️ VR | P1 | 原型截图基线 | 设计 |
| AC3 | AI 评分提供人工复核 | 🧩 API + 🖐️ Manual | P0 | 测试题库 + 评分样本 | 开发+QA |

**未产出 RTM 的需求文档**：视为不完整，不得进入步骤 2。

**🎨 原型设计约束（步骤 2.5-2.6）**

> **强制规则**：所有涉及用户界面的需求，**必须先产出可交互原型并通过评审**，才能进入文档编写和后续开发。**无原型不得开工**。

**原型保真度要求**：

| 需求类型 | 原型保真度 | 必须包含 | 工具 |
|---------|-----------|---------|------|
| **新功能模块** | 高保真（Hi-Fi） | 完整页面 + 交互流程 + 响应式（4 断点）+ Lumina 设计系统 | HTML + CSS + JS |
| **功能优化** | 中保真（Mid-Fi） | 关键页面 + 核心交互 + 视觉标注 | HTML + CSS |
| **Bug 修复** | 低保真（Lo-Fi） | 问题页面 + 修复后效果对比 | HTML 或截图标注 |
| **纯后端/接口** | 无原型要求 | API Schema + 接口文档 | OpenAPI YAML |

**原型必须覆盖的内容**：

- [ ] **核心用户流程**：至少覆盖 3 个主要用户场景（如：教师开播→学生加入→举手互动）
- [ ] **关键页面**：所有涉及用户操作的页面（登录、首页、功能页、结果页）
- [ ] **响应式适配**：Web 响应式基座必须覆盖 4 个断点（sm/md/lg/xl）
- [ ] **交互说明**：每个交互点标注触发条件、反馈效果、异常处理
- [ ] **视觉规范**：使用 Lumina 设计系统变量（颜色、字体、间距）
- [ ] **状态覆盖**：空状态、加载状态、错误状态、成功状态

**原型文件规范**：

```
原型/
├── Web/                    # Web 响应式原型（必须）
│   ├── lumina-xx-feature.html
│   └── ...
├── APP/phone/              # 移动端原型（如涉及）
├── APP/tablet/             # 平板端原型（如涉及）
└── PC/                     # 桌面端原型（如涉及）
```

**原型评审检查清单**（步骤 2.6）：

```
原型评审会议：
- [ ] 核心用户流程完整覆盖
- [ ] 交互逻辑清晰，无歧义
- [ ] 视觉风格符合 Lumina 设计系统
- [ ] 响应式适配 4 个断点正常
- [ ] 空状态/加载/错误/成功状态完整
- [ ] 可用性测试通过（≥3 人测试无明显卡点）
- [ ] 合规要求已体现（如隐私弹窗、AI 标识）
- [ ] 测试可执行性确认（AC 可通过原型验证）

评审结果：
✅ 通过 → 进入步骤 3（文档编写）
🔄 需迭代 → 返回步骤 2.5 修改后重新评审
❌ 不通过 → 返回步骤 1 重新分析需求
```

**原型与开发的追踪关系**：

```markdown
## 原型-开发追踪矩阵

| 原型文件 | 对应 AC | 开发模块 | 实现状态 |
|---------|---------|---------|---------|
| 原型/Web/lumina-live.html | AC1-AC5 | live 模块 | 🟡 开发中 |
| 原型/APP/phone/lumina-mobile-live.html | AC3-AC4 | mobile-app | ⚪ 未开始 |
```

**无原型或原型未通过评审的需求**：视为**不完整**，不得进入步骤 3（文档编写），更不得进入规划/开发。

**产出物白名单**（此工作流只能产出这些）：
- ✅ 新 HTML 文档（`原型/lumina-*.html`）
- ✅ 新原型 HTML（`原型/APP/`、`原型/Web/`、`原型/PC/`、`原型/AI/`）
- ✅ 索引页 `原型/lumina-00-index.html` 的条目更新
- ✅ README 中对应的条目/统计更新
- ❌ 其他任何文件类型 → 越界

**团队审核流程**：
```
审核前准备：
- [ ] 文档/原型已完成自测
- [ ] 所有链接可正常访问
- [ ] 视觉风格与现有文档一致
- [ ] 已更新索引页和 README
- [ ] ⚖️ 法律合规预检已完成（步骤 1.5），涉及合规域的 AC 已追加

审核内容：
- [ ] 需求覆盖完整性
- [ ] 内容准确性
- [ ] 结构逻辑性
- [ ] 视觉一致性
- [ ] 用户体验友好性
- [ ] ⚖️ 法律合规覆盖（PIPL / 未成年人 / AI / 版权 / 跨境，命中域均已标注）

审核结果：
✅ 通过 → 提交推送
🔄 需修改 → 返回步骤 3 修改后重新审核
❌ 不通过 → 返回步骤 1 重新分析需求
⚖️ 合规缺失 → 返回步骤 1.5 补齐合规 AC
```

**提交格式**：
```bash
git add -A && git commit -m "docs: 新增 xxx 文档 v1.0 (已审核)" && git push origin master
```

---

### 📐 工作流 2：规划工作流 (Planning Workflow)

**边界定义**：只产出**方案、设计、分解**类产物。只处理"怎么做、做多久、谁来做"。
**反向禁止清单**：
- 🚫 不得写业务代码（`.py` / `.ts` / `.tsx`）——那是开发工作流
- 🚫 不得直接产出最终用户可见的 HTML 原型——那是需求工作流
- 🚫 不得修改现有代码或配置——那是变更工作流

**步骤**：

| 步骤 | 操作 | 产出物 |
|------|------|--------|
| 1. 需求收集 | 明确目标、范围、约束、依赖 | 📝 需求清单 |
| **1.5 合规架构评估** | **识别需求涉及的法律合规域，设计对应的技术防护措施（加密、审计、访问控制、数据分级）** | **⚖️ 合规架构方案** |
| **1.7 原型可行性验证** | **评审需求原型（工作流 1 产出），验证技术可行性、性能风险、实现复杂度** | **🎨 原型验证报告** |
| 2. 方案设计 | 技术方案、架构设计、数据结构 | 📐 设计文档 |
| **2.5 测试策略规划** | **制定测试金字塔、测试环境、测试数据、自动化策略、准入准出标准** | **🧪 测试策略文档** |
| 3. 任务分解 | WBS 分解、时间估算、资源规划（**含测试任务**） | 📊 WBS 文档 |
| 4. 评审确认 | 方案评审、**合规评审**、**测试策略评审**、**原型验证评审**、风险评估、里程碑确认 | ✅ 评审通过 |
| 5. 文档输出 | 输出 PRD/TDD/WBS/测试策略等规划文档 | 📄 规划文档集 |
| **6. 团队审核** | **审核团队评审（含合规终审 + 测试终审 + 原型验证终审），确认通过** | **✅ 审核通过** |

**⚖️ 法律合规约束（规划阶段）**

> **强制规则**：任何涉及合规域的架构设计，**必须显式设计对应的技术防护措施**，且防护措施必须在 TDD 中独立成章（如 §合规与数据保护），否则视为规划不完整。

**合规架构设计清单**（针对架构/方案类产出）：

- [ ] **个人信息保护架构**：
  - 个人敏感信息（身份证号、生物特征、成绩）是否加密存储？（AES-256 或同等级）
  - 是否实现数据最小化原则？（只收集必要字段，不过度采集）
  - 是否支持用户"查阅-复制-更正-删除"其个人信息的权利？
  - 是否设计数据留存期限与自动删除机制？
- [ ] **未成年人保护架构**：
  - 未成年人账户是否有独立标识？（`user.role` 或 `user.age_group`）
  - 是否实现防沉迷机制？（时长限制、宵禁时段）
  - 监护人同意流程是否可追溯？（日志审计）
- [ ] **AI 合规模块设计**：
  - AI 生成内容是否带有可识别标识？（`ai_generated: boolean` + 显式提示）
  - 是否接入内容安全审核接口？（涉黄/涉政/涉暴检测）
  - 训练数据来源是否可披露？（模型池元数据）
  - AI 评分是否提供人工复核通道？（`ai_grade` 模块的 `review_status` 字段）
- [ ] **算法推荐备案设计**：
  - 推荐算法是否可关闭？（用户设置项 `user.preferences.algorithm_opt_out`）
  - 是否保留算法日志用于审计？（30 天以上留存）
- [ ] **数据分级与跨境**：
  - 数据是否按《数据安全法》分级？（一般/重要/核心）
  - 跨境传输是否走评估流程？（设计时避免默认跨境）
- [ ] **电子文书法律效力**：
  - 电子证书/成绩单是否采用可靠电子签名？（第三方 CA 或自建合规定点）
  - 签名验证流程是否完整？

**合规评审检查点**（步骤 4 评审确认必须包含）：
- [ ] TDD 包含独立的合规章节（§合规与数据保护）
- [ ] 每个合规域都有对应的技术防护措施
- [ ] 合规防护措施已估算工时（不挤占功能开发缓冲）
- [ ] 第三方合规服务（CA、内容审核、实名认证）已选型并确认预算

**命中任一合规域但未完成合规架构设计**：规划视为**不完整**，必须返回步骤 1.5 补齐。

**🎨 原型可行性验证（步骤 1.7）**

> **强制规则**：涉及用户界面的规划，**必须基于需求原型（工作流 1 产出）进行技术可行性验证**，识别性能风险、实现复杂度、技术债务。**未验证原型不得进入方案设计**。

**原型验证清单**（针对技术架构师/规划者）：

- [ ] **交互复杂度评估**：
  - 原型中的交互是否可实现？（如复杂动画、实时协作、拖拽排序）
  - 是否有现成组件库支持？（如 Ant Design、Material UI）
  - 是否需要自定义开发？预估工时？
- [ ] **性能风险评估**：
  - 原型中的数据加载量是否合理？（如列表 1000+ 项、图表大数据集）
  - 是否需要分页/虚拟滚动/懒加载？
  - 实时交互（如直播弹幕、协作编辑）的并发量预估？
- [ ] **响应式实现评估**：
  - 原型的 4 断点（sm/md/lg/xl）布局是否可实现？
  - 是否需要额外的断点或媒体查询？
  - 移动端的触摸交互是否友好？
- [ ] **技术债务识别**：
  - 原型是否要求重构现有架构？（如是，应返回工作流 2 重新规划）
  - 是否需要引入新的第三方库？评估维护成本
  - 是否有安全/合规风险？（如 XSS、CSRF、数据泄露）
- [ ] **跨平台一致性评估**：
  - Web 响应式基座能否覆盖原型的所有功能？
  - 移动端/桌面端需要哪些原生能力补位？
  - 各端的设计 token 是否统一？

**原型验证报告模板**（步骤 1.7 必须产出）：

```markdown
## 原型验证报告

### 1. 验证范围
- 原型文件：[原型路径列表]
- 验证人：[姓名]
- 验证时间：[日期]

### 2. 技术可行性

| 原型页面 | 交互复杂度 | 实现方案 | 预估工时 | 风险等级 |
|---------|-----------|---------|---------|---------|
| 首页 | 中 | 现有组件 | 2 天 | 🟢 低 |
| 直播页 | 高 | 需自定义 WebSocket | 5 天 | 🟡 中 |
| AI 评分 | 高 | 需接入 AI 网关 | 7 天 | 🔴 高 |

### 3. 性能风险

| 场景 | 数据量 | 并发量 | 风险 | 缓解方案 |
|------|--------|--------|------|---------|
| 课程列表 | 1000+ | 100 QPS | 🟡 中 | 分页 + 缓存 |
| 直播弹幕 | 实时 | 500 并发 | 🔴 高 | WebSocket 分片 |

### 4. 技术债务

- [ ] 需要重构：[模块列表 + 原因]
- [ ] 需要新引入：[第三方库 + 评估]
- [ ] 安全风险：[风险点 + 缓解方案]

### 5. 结论

✅ 原型可行，进入方案设计
🔄 原型需调整：[具体调整建议] → 返回工作流 1
❌ 原型不可行：[原因] → 返回工作流 1 重新设计
```

**原型验证结论处理**：

| 结论 | 处理 |
|------|------|
| ✅ 可行 | 进入步骤 2（方案设计） |
| 🔄 需调整 | 返回工作流 1（需求），迭代原型后重新验证 |
| ❌ 不可行 | 返回工作流 1（需求），重新设计或拆分需求 |

**未产出原型验证报告的规划**：视为**不完整**，不得进入步骤 2（方案设计）。

**🧪 测试策略规划（步骤 2.5）**

> **强制规则**：每个技术方案必须配套测试策略，**没有测试策略的技术方案不得进入评审**。测试策略必须在 TDD 中独立成章（§测试工程）。

**测试金字塔设计**（按 Lumina 技术栈适配）：

```
              ╱ ╲
             ╱ E2E╲           ~5% · Playwright 关键流程
            ╱──────╲
           ╱ 集成测试 ╲        ~15% · pytest + httpx + TestClient
          ╱────────────╲
         ╲   单元测试    ╱      ~70% · pytest 函数/类级测试
          ╲────────────╱
           ╲ 静态检查  ╱        ~10% · mypy + ruff + bandit
            ╲────────╱
```

**测试策略六要素**（每个技术方案必须明确）：

| 要素 | 必须回答的问题 | 输出 |
|------|--------------|------|
| **1. 测试范围** | 哪些模块/功能需要测试？哪些可豁免？ | 测试范围清单（含豁免说明） |
| **2. 测试类型** | 单元/集成/E2E/性能/安全/合规各占多少？ | 测试金字塔比例 |
| **3. 测试环境** | Dev / Staging / Prod 如何配置？数据如何隔离？ | 环境拓扑图 |
| **4. 测试数据** | 如何生成、脱敏、重置？Mock 策略？ | 测试数据方案 |
| **5. 自动化策略** | 哪些测试必须自动化？哪些手工？CI 集成点？ | 自动化覆盖率目标 |
| **6. 准入准出** | 进入测试的前置条件？发布的退出标准？ | 准入/准出清单 |

**测试环境规划**（Lumina 标准三环境）：

```
┌─────────────┐      ┌──────────────┐      ┌──────────────┐
│  Dev 环境    │  →   │  Staging 环境 │  →   │  Prod 环境   │
│  开发自测    │      │  集成测试     │      │  生产发布    │
│             │      │              │      │              │
│ • 本地 MySQL│      │ • Docker 容器 │      │ • 正式集群   │
│ • seed_demo │      │ • 脱敏数据    │      │ • 真实数据   │
│ • Mock 外部 │      │ • 真实外部    │      │ • 真实外部   │
└─────────────┘      └──────────────┘      └──────────────┘
```

**测试数据方案**（必答题）：

- [ ] **数据生成**：fixture / factory / seed 脚本（`部署/scripts/seed_demo.py` 模式）
- [ ] **数据脱敏**：Staging 环境不得直接使用 Prod 真实数据
- [ ] **数据隔离**：每个测试用例使用独立事务或临时数据库
- [ ] **数据重置**：CI 每次运行前重置测试数据
- [ ] **Mock 策略**：外部服务（AI 网关、邮件、支付）使用 Mock / Fake

**自动化策略**（覆盖率目标）：

| 测试层级 | 覆盖率目标 | CI 集成 | 失败阻断 |
|---------|-----------|---------|---------|
| 单元测试 | ≥ 80%（核心模块 ≥ 90%） | 每次提交 | ✅ 阻断合并 |
| 集成测试 | ≥ 70%（API 路径覆盖） | 每次 PR | ✅ 阻断合并 |
| E2E 测试 | 关键流程 100%（登录/直播/答题/批阅） | 每次发版 | ✅ 阻断发布 |
| 视觉回归 | 核心页面 100%（响应式 4 断点） | 每次 PR | 🟡 警告不阻断 |
| 性能测试 | 核心接口 P95 < 500ms | 每周基准 | 🟡 趋势监控 |
| 安全测试 | OWASP Top 10 覆盖 | 每次发版 | ✅ 阻断发布 |
| 合规测试 | 命中合规域 100% | 每次发版 | ✅ 阻断发布 |

**准入准出标准**：

**测试准入（进入 Staging 前）**：
- [ ] 单元测试 100% 通过
- [ ] 集成测试 100% 通过
- [ ] 代码审查已完成
- [ ] 合规自检已完成
- [ ] 测试数据已准备

**测试准出（进入 Prod 前）**：
- [ ] E2E 测试 100% 通过
- [ ] 视觉回归无未批准差异
- [ ] 性能无回归（P95 劣化 ≤ 10%）
- [ ] 安全扫描无高危/严重
- [ ] 合规测试 100% 通过
- [ ] 遗留缺陷已评估（无 P0/P1）
- [ ] 测试报告已归档

**测试策略文档模板**（TDD §测试工程章节结构）：

```markdown
# §测试工程

## 1. 测试范围
   - 在范围：[模块列表]
   - 豁免：[模块 + 豁免理由]

## 2. 测试金字塔
   - 单元/集成/E2E/性能/安全/合规 比例

## 3. 测试环境
   - Dev / Staging / Prod 配置

## 4. 测试数据
   - 生成/脱敏/隔离/重置/Mock 方案

## 5. 自动化策略
   - 各层级覆盖率目标 + CI 集成点

## 6. 准入准出标准
   - 进入 Staging 条件
   - 进入 Prod 条件

## 7. 测试任务 WBS
   - 测试设计工时
   - 测试实现工时
   - 测试执行工时
```

**未包含测试策略章节的技术方案**：视为**不完整**，必须返回步骤 2.5 补齐。

**产出物白名单**：
- ✅ 规划类 HTML 文档（PRD / TDD / WBS / 运维手册等的章节）
- ✅ 架构图、流程图、数据结构设计（可嵌入 HTML）
- ✅ OpenAPI YAML 的 schema 设计（**仅 schema，不含路由实现**）
- ❌ 业务代码 / 单元测试 / 原型页面 → 越界

**团队审核流程**：
```
审核前准备：
- [ ] 规划文档已完成自检
- [ ] 技术方案已验证可行性
- [ ] WBS 任务分解完整
- [ ] 时间估算有依据
- [ ] ⚖️ 合规架构评估已完成（步骤 1.5），TDD 含独立合规章节

审核内容：
- [ ] 技术方案合理性
- [ ] 架构设计可扩展性
- [ ] 任务分解完整性
- [ ] 时间估算准确性
- [ ] 风险应对措施
- [ ] 资源分配合理性
- [ ] ⚖️ 法律合规架构完整性（加密/审计/访问控制/数据分级/CA 认证，命中域均已设计）

审核结果：
✅ 通过 → 进入开发执行阶段
🔄 需调整 → 返回步骤 2-3 调整后重新审核
❌ 不通过 → 返回步骤 1 重新收集需求
⚖️ 合规架构缺失 → 返回步骤 1.5 补齐技术防护措施
```

---

### 🔨 工作流 3：开发执行工作流 (Development Workflow)

**边界定义**：只产出**业务代码 + 测试代码**。只处理"把方案变成可运行代码"。
**反向禁止清单**：
- 🚫 不得新建 PRD / TDD / WBS / 原型 HTML 文件——那是需求/规划工作流
- 🚫 不得做架构级变更（如新增模块、拆分服务）——那是规划工作流
- 🚫 不得修改已有文档的非代码章节（如 API 文档措辞）——那是变更工作流
- 🚫 不得在验证阶段继续修代码——验证流接管

**步骤**：

| 步骤 | 操作 | 检查点 |
|------|------|--------|
| 1. 环境准备 | 确认依赖、配置、开发环境 | ✅ 环境就绪 |
| 2. 代码编写 | 实现功能、编写逻辑、处理异常 | ✅ 代码规范 |
| 3. 单元测试 | 编写测试、运行测试、覆盖率检查 | ✅ 测试通过 |
| 4. 集成测试 | 端到端测试、接口测试、性能测试 | ✅ 集成正常 |
| 5. 代码审查 | 自审代码、优化重构、文档注释 | ✅ 质量达标 |
| 6. 开发进度更新 | 更新 `开发进度.md`：任务状态、提交 ID、验证结果 | ✅ 进度同步 |
| 7. 提交推送 | 增量提交、清晰注释、及时推送 | ✅ 版本可追溯 |
| **8. 团队审核** | **审核团队评审，确认通过** | **✅ 审核通过** |

**产出物白名单**：
- ✅ `服务/lumina-app/app/` 下的 `.py` 业务代码
- ✅ `服务/lumina-app/tests/` 下的测试代码
- ✅ `服务/web-frontend/src/` 或 `服务/mobile-app/src/` 的前端代码
- ✅ `scripts/` 下的工具脚本
- ✅ `服务/lumina-app/requirements.txt` 依赖变更
- ✅ `服务/lumina-app/.env.example` 等配置模板
- ✅ `开发进度.md` 进度同步
- ❌ `原型/` 目录下任何 HTML → 越界
- ❌ `原型/lumina-prd.html` / `lumina-tdd.html` 章节内容 → 越界

**开发规范**：
- 📁 脚本放在 `scripts/` 目录
- 🐍 Python 脚本遵循 PEP 8
- 📝 函数/类必须有文档字符串
- 🧪 核心功能必须有单元测试
- 🔒 敏感信息使用环境变量
- 📈 **每个 WBS 任务完成后必须更新 `开发进度.md`**（与代码同次提交），不允许带着积压进度提交

**🎯 开发优先级顺序（强制 · Sprint 规划与任务排期必须遵循）**

> **核心原则**：自底向上、由内而外。先让系统跑得起来（基础能力），再让管理员能管（管理端），再让老师能教（教师端），最后让学生能用（学生端）。前端按覆盖面递减：Web 优先承载全部能力，移动/桌面/平板依次补位。

**维度 A：功能层优先级（后端 + 全栈逻辑）**

```
P1 基础能力 ──▶ P2 管理端 ──▶ P3 教师端 ──▶ P4 学生端
（基座）       （管控）       （生产力）     （消费端）
```

| 优先级 | 含义 | 典型产出 | 说明 |
|--------|------|---------|------|
| **P1 基础能力** | 平台基座，所有上层依赖的公共能力 | user 认证、course 模型、file 上传、permission 权限、notif 基础通道、events 事件总线、logs 审计、数据库迁移、部署脚本 | 缺失则 P2/P3/P4 全部无法工作；**任何 Sprint 必须先确保 P1 完整** |
| **P2 管理端** | 管理员视角的管控能力 | 模型池管理、课程审核、用户管理、数据统计后台、系统配置、运维入口 | 没有管控能力，教师/学生端上线后无法运营；**P1 完成后立刻推进 P2** |
| **P3 教师端** | 教师生产力工具 | 课程创建、直播开播、点名、答题发布、作业批阅、学情分析 | 教学的"供给侧"，必须先有教师端才有学生可消费的内容 |
| **P4 学生端** | 学生消费侧体验 | 选课、进入直播、举手、答题、作业提交、学习档案查看 | 教学的"消费侧"，**仅在 P1-P3 对应能力就绪后开发** |

**排期硬性规则**：
- 🚫 **禁止跳层**：P1 未完成不得开发 P2，P2 未完成不得开发 P3，P3 未完成不得开发 P4
- ✅ **允许并行**：P1 已完成的前提下，P2 与 P3 可小幅并行（≤ 20% 容量用于 P3 前置准备）
- ✅ **允许补齐**：为修复 P4 Bug 而改动 P1/P2/P3 代码 → 走工作流 5（变更），不受此顺序约束
- 📋 **Backlog 排序**：所有用户故事按 P1 > P2 > P3 > P4 分组，Sprint 规划优先拉满高优先级

**维度 B：前端平台优先级（响应式基座 + 原生能力补位）**

> **核心原则**：Web 为**响应式基座**，一套代码适配 PC / 平板 / 手机视口，承载全部功能；其他端仅在 Web 无法提供的**原生能力**（推送、相机、系统托盘、大屏触控优化等）上叠加，不重复造轮子。

```
Web（响应式）──▶ 移动端（原生能力）──▶ 桌面端（系统集成）──▶ 平板端（大屏深度优化）
  全功能基座        补齐原生特性           OS 级集成            触控 + 多栏布局
```

| 优先级 | 平台 | 目录 | 定位与覆盖策略 |
|--------|------|------|---------------|
| **F1 Web（响应式）** | 浏览器端（PC / 平板 / 手机视口） | `服务/web-frontend/` | **响应式基座**：一套代码覆盖所有视口（desktop / tablet / mobile breakpoints），承载 100% 功能，作为**功能基准与视觉基准**；其他端均以 Web 为对齐目标 |
| **F2 移动端** | iOS / Android 原生（RN/Expo） | `服务/mobile-app/` | **原生能力补位**：补齐 Web 做不到的原生特性——推送通知（APNs/FCM）、相机深度集成、离线缓存、生物识别、后台任务；**UI 与交互与 Web 响应式移动版对齐，不重做功能** |
| **F3 桌面端** | Electron（规划中） | `原型/PC/` → 后续实现 | **系统集成补位**：系统托盘、全局快捷键、文件关联、本地文件读写、大屏多窗口；**渲染层直接复用 Web 响应式代码**，仅包装原生壳 |
| **F4 平板端** | 大屏触控深度优化 | `原型/APP/tablet/` | **大屏深度优化**：在 Web 响应式平板断点之上，增加多栏布局、触控手势、Apple Pencil / 手写笔支持、分屏协作；**不引入新功能，只做体验升级** |

**关键澄清**：
- ✅ **Web 响应式 ≠ 只做 PC**：Web 一个代码库通过 CSS 断点（`sm/md/lg/xl/2xl`）同时覆盖手机、平板、桌面视口
- ✅ **移动端 ≠ 重做一套功能**：移动端功能集是 Web 的子集 + 原生特性，业务逻辑与接口契约与 Web 完全一致
- ✅ **桌面端 ≠ 独立开发**：Electron 直接加载 Web 响应式页面，仅补充 OS 集成 API
- ✅ **平板端 ≠ 新端**：平板端是 Web 响应式在 `lg/xl/2xl` 断点的深度打磨 + 触控手势增强

**排期硬性规则**：
- 🚫 **禁止在 Web 响应式未覆盖某视口前，单独开发该视口的原生端**：例——Web 平板断点未打磨前，不得启动 F4 平板端优化
- 🚫 **禁止原生端实现 Web 已覆盖的功能**：所有功能必须先在 Web 响应式中实现，原生端仅补齐"Web 做不到"的原生特性
- 🚫 **禁止跨平台同步开发同一功能**：一个功能必须先在 Web 上线稳定后，再在原生端补齐其原生特性
- ✅ **允许复用**：移动端/桌面端应尽可能复用 Web 组件、接口契约、设计 token
- 📊 **功能对齐基准**：每完成一个功能，记录其在 4 个平台的覆盖状态（参见 `原型/lumina-status-report.html`）

**响应式断点与平台对应（参考）**：

```
    手机         平板          桌面          大屏
  < 640px    640-1024px    1024-1440px    > 1440px
   [sm]        [md/lg]       [xl]           [2xl]
     │            │             │              │
     └────────────┴─────────────┴──────────────┘
                     │
              F1 Web 响应式基座
              （一套代码全覆盖）
                     
  额外叠加：  F2 移动端原生   F3 桌面集成   F4 平板深优化
             (原生特性)      (OS 集成)     (触控+多栏)
```

**维度 A × B 组合矩阵（任务落地参考）**：

|  | F1 Web（响应式基座） | F2 移动端（原生） | F3 桌面端（集成） | F4 平板端（深优化） |
|--|----------------------|------------------|------------------|--------------------|
| **P1 基础能力** | ✅ 先做（全视口） | ✅ 跟随（原生推送等） | ✅ 跟随（壳工程） | ✅ 跟随（断点验证） |
| **P2 管理端** | ✅ 第二做 | 🟡 按需（管理员移动审批） | 🟡 按需 | 🟡 按需 |
| **P3 教师端** | ✅ 第三做 | ✅ 直播/点名等原生场景 | ⏳ 暂缓 | ⏳ 暂缓 |
| **P4 学生端** | ✅ 第四做 | ✅ 核心场景（答题/举手） | ⏳ 暂缓 | ⏳ 暂缓 |

> 说明：单元格 ✅ 表示该交叉点为高优先级开发项；🟡 表示视容量按需补位；⏳ 表示延后到前置平台稳定。F1 列的"全视口"表示一次实现覆盖手机/平板/桌面三个断点。

**优先级与敏捷节奏的衔接**：
- **Sprint 规划时**：Backlog 先按 P1→P2→P3→P4 排序，同一优先级内再按 F1→F2→F3→F4 排序
- **DoD 中追加**：一个功能在 Web 响应式（F1）所有断点（sm/md/lg/xl）未全部通过前，不得标记 Done 进入其他端任务
- **速度统计**：每 Sprint 记录各优先级完成占比，健康分布应为 P1 ≥ 30% → P2 ≥ 20% → P3 ≥ 30% → P4 ≤ 20%（项目前期）；后期逐步向 P3/P4 倾斜
- **全景视图**：跨维度进度汇总参见 `原型/lumina-status-report.html`（项目全景状态报告），含里程碑/模块/技术栈/质量/风险

**团队审核流程**：
```
审核前准备：
- [ ] 代码已完成自测
- [ ] 单元测试覆盖率达标（≥80%）
- [ ] 集成测试通过
- [ ] 代码已自审优化
- [ ] 文档注释完整
- [ ] 开发进度.md 已更新（任务状态 + 提交 ID + 验证结果）

审核内容：
- [ ] 代码质量（规范性、可读性）
- [ ] 功能完整性
- [ ] 测试覆盖率
- [ ] 性能表现
- [ ] 安全性检查
- [ ] 文档完整性
- [ ] 进度记录准确性（开发进度.md 与提交一致）

审核结果：
✅ 通过 → 合并代码，进入验证阶段
🔄 需修改 → 返回步骤 2-5 修改后重新审核
❌ 不通过 → 重新评估方案，返回步骤 1
```

**增量提交示例**：
```bash
# 完成一个功能模块
git add scripts/xxx.py
git commit -m "feat: 实现 xxx 功能"
git push origin master

# 添加测试
git add tests/test_xxx.py
git commit -m "test: 添加 xxx 单元测试"
git push origin master

# 同步 WBS 进度（必须）
git add 开发进度.md
git commit -m "docs: WBS x.x 完成：xxx 功能，提交 ID 跟踪"
git push origin master

# 审核通过后合并
git commit -m "release: xxx 功能已通过团队审核"
git push origin master
```

---

### ✅ 工作流 4：收尾验证工作流 (Verification Workflow)

**边界定义**：只做**检查、核对、验收、测试执行**。不产出任何新代码或新内容。
**反向禁止清单（最严格）**：
- 🚫 **不得修复任何 Bug**——发现缺陷记录后转交工作流 3/5
- 🚫 **不得新增功能或内容**——那是需求/开发工作流
- 🚫 **不得重构或调整代码**——那是变更工作流
- 🚫 **不得修改文档正文**——那是变更工作流
- 🚫 唯一允许的"修改"：索引/README/版本号等**元数据**同步

**步骤**：

| 步骤 | 操作 | 检查点 |
|------|------|--------|
| 1. 测试计划制定 | 根据 RTM（工作流 1 产出）+ 测试策略（工作流 2 产出）制定本次验证测试计划 | 🧪 测试计划已定 |
| 2. 测试环境准备 | 确认 Staging 环境与测试数据就绪 | ✅ 环境就绪 |
| 3. 测试执行 | 按测试金字塔分层执行：单元→集成→E2E→视觉→性能→安全→合规 | 🧪 测试执行完成 |
| 4. 完整性检查 | 所有计划内容是否完成（对照 WBS） | ✅ 内容完整 |
| 5. 一致性检查 | 视觉风格、术语、版本号一致 | ✅ 风格统一 |
| 6. 链接检查 | 所有链接可访问、无死链 | ✅ 链接有效 |
| 7. 索引同步 | 索引页、README、统计数字更新 | ✅ 索引最新 |
| 8. 测试报告归档 | 输出测试报告 + 缺陷清单 + 度量指标 | 📊 报告已归档 |
| 9. 最终提交 | 完整提交信息，标注版本号 | ✅ 版本清晰 |
| 10. 交付确认 | GitHub 检查、浏览器验证 | ✅ 交付成功 |
| **11. 团队审核** | **审核团队最终验收（含测试报告评审）** | **✅ 审核通过** |

**🧪 系统化测试工程（步骤 1-3, 8）**

> **强制规则**：验证工作流必须按工作流 1 的 RTM + 工作流 2 的测试策略执行，**不得跳过任何测试层级**，**不得无报告通过**。

**测试执行分层流程**（按测试金字塔顺序）：

```
┌─────────────────────────────────────────────────────────┐
│  Layer 7: 合规测试        ⚖️  命中合规域 100% 通过     │
├─────────────────────────────────────────────────────────┤
│  Layer 6: 安全测试        🔒  OWASP Top 10 无高危       │
├─────────────────────────────────────────────────────────┤
│  Layer 5: 性能测试        📊  P95 < 500ms 无回归        │
├─────────────────────────────────────────────────────────┤
│  Layer 4: 视觉回归        👁️  核心页面 4 断点无差异     │
├─────────────────────────────────────────────────────────┤
│  Layer 3: E2E 测试        🤖  关键流程 100% 通过        │
├─────────────────────────────────────────────────────────┤
│  Layer 2: 集成测试        🧩  API 路径 ≥ 70% 通过       │
├─────────────────────────────────────────────────────────┤
│  Layer 1: 单元测试        🧪  覆盖率 ≥ 80% 通过         │
└─────────────────────────────────────────────────────────┘
```

**测试执行命令（Lumina 标准）**：

```bash
# Layer 1: 单元测试
cd 服务/lumina-app && pytest tests/ --cov=app --cov-report=term-missing -q
# 期望：≥ 80% 覆盖率，0 失败

# Layer 2: 集成测试
pytest tests/integration/ -v --tb=short
# 期望：0 失败

# Layer 3: E2E 测试
cd 服务/web-frontend && npx playwright test
# 期望：关键流程 100% 通过

# Layer 4: 视觉回归
npx playwright test --project=visual-regression
# 期望：4 断点（sm/md/lg/xl）无未批准差异

# Layer 5: 性能测试
cd 部署/scripts && ./perf_baseline.sh
# 期望：P95 < 500ms，无回归

# Layer 6: 安全测试
cd 服务/lumina-app && bandit -r app/ && pip-audit
# 期望：无高危/严重漏洞

# Layer 7: 合规测试
pytest tests/compliance/ -v
# 期望：命中合规域 100% 通过
```

**测试报告模板**（步骤 8 必须输出）：

```markdown
# 测试报告 v1.x · YYYY-MM-DD

## 1. 测试概览
- 测试范围：[本轮验证覆盖的模块/功能]
- 测试环境：Staging (Docker) · MySQL 9.7 · Python 3.11
- 测试执行人：[姓名]
- 测试执行时间：[起止时间]

## 2. 测试金字塔结果

| 测试层级 | 用例数 | 通过 | 失败 | 跳过 | 通过率 | 覆盖率 |
|---------|--------|------|------|------|--------|--------|
| L1 单元测试 | X | X | 0 | 0 | 100% | 82% |
| L2 集成测试 | X | X | 0 | 0 | 100% | 75% |
| L3 E2E 测试 | X | X | 0 | 0 | 100% | - |
| L4 视觉回归 | X | X | 0 | 0 | 100% | - |
| L5 性能测试 | X | X | 0 | 0 | 100% | P95=320ms |
| L6 安全测试 | X | X | 0 | 0 | 100% | - |
| L7 合规测试 | X | X | 0 | 0 | 100% | - |

## 3. 缺陷清单

| ID | 严重度 | 描述 | 复现步骤 | 状态 | 责任方 |
|----|--------|------|---------|------|--------|
| BUG-001 | P0 | [描述] | [步骤] | Open | 工作流 5 |
| BUG-002 | P2 | [描述] | [步骤] | Open | 工作流 5 |

严重度定义：
- P0: 阻塞发布（核心流程不可用 / 数据丢失 / 安全漏洞）
- P1: 严重（主要功能不可用 / 性能严重劣化）
- P2: 一般（次要功能异常 / UI 瑕疵）
- P3: 轻微（文案错误 / 优化建议）

## 4. 遗留缺陷评估
- P0 遗留：0 个
- P1 遗留：0 个
- P2 遗留：X 个（已评估，可延后）
- P3 遗留：X 个（已评估，可延后）

## 5. 准出判定
- [x] 所有 P0/P1 缺陷已修复
- [x] 测试覆盖率达标
- [x] 性能无回归
- [x] 安全无高危
- [x] 合规 100% 通过
- **结论：✅ 准许发布 / ❌ 打回修复**
```

**测试度量指标**（每轮验证必须统计）：

| 指标 | 健康阈值 | 警告阈值 | 阻断阈值 |
|------|---------|---------|---------|
| 单元测试覆盖率 | ≥ 85% | 75-85% | < 75% |
| 集成测试覆盖率 | ≥ 75% | 60-75% | < 60% |
| E2E 通过率 | 100% | 95-99% | < 95% |
| P95 响应时间 | < 500ms | 500-800ms | > 800ms |
| P0/P1 缺陷数 | 0 | 1-2 | ≥ 3 |
| 安全高危漏洞 | 0 | 1 | ≥ 2 |
| 合规测试通过率 | 100% | - | < 100% |

**发现缺陷的处理**：
- P0/P1 缺陷 → **立即停工**，转工作流 5（变更）修复，修复后**重新进入验证流**
- P2/P3 缺陷 → 记录到缺陷清单，评估可延后，**不阻塞发布**
- 功能缺失 → 转工作流 3（开发）补全
- 文档缺失 → 转工作流 1（需求）补文档
- 合规未达标 → 转工作流 5（变更）补齐，重新验证

**验证检查清单**：
- [ ] 所有计划内容已完成
- [ ] 视觉风格完全一致
- [ ] 所有功能可正常使用
- [ ] 所有链接可正常访问
- [ ] 索引页已更新
- [ ] README 已更新
- [ ] 版本号已更新
- [ ] 统计数据准确
- [ ] 测试全部通过
- [ ] 提交信息完整清晰
- [ ] 远程仓库已同步

**团队审核流程**：
```
审核前准备：
- [ ] 所有验证检查项已通过
- [ ] 文档/代码已提交到远程仓库
- [ ] 版本号已正确标注
- [ ] 变更日志已编写

审核内容：
- [ ] 交付物完整性
- [ ] 质量标准达成
- [ ] 文档规范性
- [ ] 用户体验
- [ ] 上线准备度
- [ ] 回滚方案（如适用）

审核结果：
✅ 通过 → 正式发布，项目收尾
🔄 需完善 → 记录问题清单，转交工作流 3/5 修复，修复后重入本流
❌ 不通过 → 重新评估，可能需要返工
```

**最终提交格式**：
```bash
git commit -m "release: 完成 xxx v1.0 (已通过团队审核)

变更内容：
- 新增 xxx 功能
- 完成 xxx 章节
- 修复 xxx 问题

验证结果：
- ✅ 完整性检查通过
- ✅ 功能测试通过
- ✅ 链接检查通过
- ✅ 索引同步完成
- ✅ 团队审核通过

版本号：v1.0
文档编号：DOC-LUMINA-2026-xxx"
```

---

### 🔄 工作流 5：变更工作流 (Change Workflow)

**边界定义**：只**修改已存在的文件**。不新增、不重构架构。
**反向禁止清单**：
- 🚫 **不得新增文件**——新增任何文件 = 需求工作流（工作流 1）或开发工作流（工作流 3）
- 🚫 **不得做架构级重构**——那是规划工作流（工作流 2）
- 🚫 **不得借机加功能**（修 Bug 时顺手加特性）——新功能走开发工作流
- 🚫 **不得在验证流中执行**——验证流发现的问题才转这里，但执行完要回到验证流

**步骤**：

| 步骤 | 操作 | 检查点 |
|------|------|--------|
| **0. 变更分级** | **评估变更级别（S/M/L/XL），确定流程严格度** | **📏 级别已定** |
| 1. 变更分析 | 明确变更对象、变更目标、**影响范围**（关联文件、索引、版本号、调用方） | ✅ 变更清晰 |
| **1.5 影响半径检查** | **确认变更文件数、行数、模块数未超出该级别的半径限制** | **📏 半径合规** |
| 2. 影响评估 | 确认是否需要同步：版本号升级、索引页、README、开发进度、其它依赖模块 | ✅ 影响全覆盖 |
| 3. 方案实施 | 采用与既有产物一致的风格/约定进行修改；涉及数据结构/接口变动的先说明兼容性 | ✅ 风格一致 |
| 4. 回归验证 | 原有功能可用 + 新变更生效；相关测试、链接、埋点不受影响（**按级别执行对应回归测试**） | ✅ 无回归 |
| 5. 联动同步 | 更新受影响的索引、README、版本历史、`开发进度.md`（如涉及） | ✅ 联动完成 |
| 6. 提交推送 | 提交信息记录「变更前 → 变更后」，清晰可追溯 | ✅ 版本可追溯 |
| **7. 团队审核** | **审核团队评审变更必要性与质量，确认通过（按级别决定审核严格度）** | **✅ 审核通过** |

**📏 变更分级制度（步骤 0）**

> **强制规则**：所有变更必须先分级，**不同级别走不同流程**。未分级的变更视为违规，必须返回步骤 0。

| 级别 | 定义 | 文件数 | 代码行数 | 模块数 | 流程严格度 |
|------|------|--------|---------|--------|-----------|
| **S（Small）** | 微调：文案、样式、配置、单文件小修 | 1-2 | ≤ 20 | 1 | 自审通过即可 |
| **M（Medium）** | 标准变更：Bug 修复、小功能调整、多文件联动 | 3-5 | 21-100 | 1-2 | 团队审核（快速） |
| **L（Large）** | 重大变更：接口变动、数据结构调整、跨模块影响 | 6-10 | 101-300 | 2-4 | 团队审核（完整） + 回归测试报告 |
| **XL（X-Large）** | 超大型变更：接近重构、高风险、影响核心流程 | >10 | >300 | >4 | **禁止走变更流**，必须拆分或转工作流 2（规划） |

**分级判定流程**：

```
变更请求
  │
  ├─ 文件数 > 10 或 行数 > 300 或 模块数 > 4？
  │   └─ ✅ 是 → XL 级别 → 🚫 禁止走变更流，转工作流 2
  │
  ├─ 文件数 6-10 或 行数 101-300 或 模块数 2-4？
  │   └─ ✅ 是 → L 级别 → 完整审核 + 回归测试报告
  │
  ├─ 文件数 3-5 或 行数 21-100 或 模块数 1-2？
  │   └─ ✅ 是 → M 级别 → 快速审核
  │
  └─ 文件数 1-2 且 行数 ≤ 20 且 模块数 1？
      └─ ✅ 是 → S 级别 → 自审通过
```

**📏 影响半径约束（步骤 1.5）**

> **强制规则**：变更的影响半径必须与级别匹配，**超出半径视为范围溢出**，必须返回步骤 0 重新分级或拆分。

| 级别 | 文件数上限 | 行数上限 | 模块数上限 | 影响半径约束 |
|------|-----------|---------|-----------|-------------|
| **S** | 2 | 20 | 1 | 仅限目标文件 + 直接引用的配置/索引 |
| **M** | 5 | 100 | 2 | 目标文件 + 直接依赖模块 + 索引/README |
| **L** | 10 | 300 | 4 | 目标文件 + 依赖模块 + 调用方 + 索引/README/版本历史 |
| **XL** | - | - | - | 🚫 禁止走变更流 |

**半径溢出处理**：
- 实施过程中发现文件数/行数/模块数超出当前级别 → **立即停工**，返回步骤 0 重新分级
- 超出 L 级别 → 必须拆分为多个 M/L 变更，或转工作流 2（规划）
- 拆分后每个子变更独立走变更流，各自有独立的提交和审核

**🧪 回归测试策略（步骤 4，按级别）**

| 级别 | 回归测试要求 | 测试报告 |
|------|-------------|---------|
| **S** | 自验：目标功能正常 + 无明显视觉问题 | ❌ 不需要 |
| **M** | 单测通过 + 受影响模块的集成测试通过 | 📝 简要说明（提交信息中） |
| **L** | 单测 + 集成 + E2E（关键流程）+ 视觉回归（核心页面） | 📊 完整测试报告（附录） |
| **XL** | 🚫 禁止走变更流 | - |

**L 级别回归测试报告模板**（附加在提交信息或 PR 描述中）：

```markdown
## 回归测试报告

### 测试范围
- 修改模块：[模块列表]
- 影响模块：[依赖/调用方模块]

### 测试结果

| 测试层级 | 用例数 | 通过 | 失败 | 备注 |
|---------|--------|------|------|------|
| 单元测试 | X | X | 0 | - |
| 集成测试 | X | X | 0 | 受影响 API |
| E2E 测试 | X | X | 0 | 关键流程 |
| 视觉回归 | X | X | 0 | 核心页面 |

### 结论
✅ 回归测试全部通过，无引入新缺陷
```

**🧊 变更冻结规则**

> **强制规则**：以下场景**禁止任何变更**，必须等待冻结期结束或走特殊审批流程。

| 冻结场景 | 冻结时长 | 例外 |
|---------|---------|------|
| **发版前 24 小时** | 24 小时 | P0 级 Hotfix（需团队负责人审批） |
| **Sprint 最后 1 天** | 1 天 | 无例外（保证 Sprint 稳定收尾） |
| **重大演示/评审前** | 演示前 4 小时 | P0 级 Hotfix |
| **生产环境故障中** | 故障修复前 | 仅允许故障相关修复 |
| **合规审计期间** | 审计期间 | 仅允许审计要求的修复 |

**冻结期违规变更**：视为**严重违规**，必须回滚并重新评估。

**🔄 回滚方案（必须提前准备）**

> **强制规则**：L 级别及以上变更必须在实施前准备回滚方案，**无回滚方案不得实施**。

**回滚方案模板**（L 级别必须在步骤 1 产出）：

```markdown
## 回滚方案

### 回滚触发条件
- [ ] 回归测试失败
- [ ] 生产环境出现 P0/P1 缺陷
- [ ] 性能劣化 > 20%
- [ ] 其他：[具体条件]

### 回滚步骤
1. `git revert <commit-hash>` 或 `git reset --hard <previous-commit>`
2. 验证回滚后功能正常
3. 通知相关方
4. 记录回滚原因

### 数据回滚（如涉及数据库变更）
- [ ] 备份数据：`mysqldump -u root lumina > backup_$(date +%Y%m%d).sql`
- [ ] 回滚 SQL：`source rollback_script.sql`
- [ ] 验证数据一致性
```

**⚠️ 范围溢出处理（变更过程中）**

> **强制规则**：变更过程中发现范围扩大，**必须立即停工**，不得"顺手"完成。

**范围溢出判定**：
- 文件数超出当前级别上限
- 行数超出当前级别上限
- 模块数超出当前级别上限
- 发现需要新增功能（不是修复）
- 发现需要重构架构

**范围溢出处理流程**：

```
变更实施中
  │
  ├─ 发现范围溢出？
  │   │
  │   ├─ ✅ 是 → 立即停工
  │   │   │
  │   │   ├─ 评估溢出程度
  │   │   │   │
  │   │   │   ├─ 溢出到 XL → 转工作流 2（规划）
  │   │   │   │
  │   │   │   ├─ 溢出到 L（原为 M） → 返回步骤 0 重新分级为 L
  │   │   │   │
  │   │   │   └─ 发现新功能需求 → 拆分为：
  │   │   │       ├─ 当前变更流：完成原变更目标
  │   │   │       └─ 新工作流 3（开发）：实现新功能
  │   │   │
  │   │   └─ 提交已完成的变更（如已可提交）
  │   │
  │   └─ ❌ 否 → 继续实施
  │
  └─ 完成变更
```

**范围溢出违规**：发现范围溢出但未停工，继续"顺手"完成 → 视为**严重违规**，变更必须回滚并重新评估。

**产出物白名单**：
- ✅ 对**已存在文件**的修改（git diff 显示的全部是 modify，无 new file）
- ✅ 联动更新的索引/README/版本历史/开发进度
- ❌ `git status` 出现 `??` 未跟踪的新文件 → 越界（应走需求/开发工作流）

**变更提交格式**：
```bash
git commit -m "change: 修改 xxx（v1.x → v1.x+1）

变更前：...
变更后：...
影响范围：xxx / 索引 / README / 开发进度.md
验证结果：✅ 回归通过
审核：Claude 团队审核通过"
```

**团队审核流程（按级别）**：

**S 级别（自审）**：
```
自审检查项：
- [ ] 变更目标明确（单文件小修）
- [ ] 变更已实施且风格一致
- [ ] 自验通过（目标功能正常）
- [ ] 无明显视觉问题

自审结果：
✅ 通过 → 直接提交推送
❌ 不确定 → 升级为 M 级别，走团队审核
```

**M 级别（快速团队审核）**：
```
审核前准备：
- [ ] 变更目标明确
- [ ] 影响范围已评估（文件 + 直接依赖）
- [ ] 变更已实施且风格一致
- [ ] 单测 + 受影响模块集成测试通过

审核内容：
- [ ] 变更必要性（是否有更小改动方案）
- [ ] 变更准确性
- [ ] 影响评估完整性

审核结果：
✅ 通过 → 提交推送
🔄 需调整 → 返回步骤 3-4 调整后重新审核
❌ 不通过 → 返回步骤 1 重新分析变更
```

**L 级别（完整团队审核）**：
```
审核前准备：
- [ ] 变更目标明确
- [ ] 影响范围已完整评估（文件 + 依赖模块 + 调用方）
- [ ] 变更已实施且风格一致
- [ ] 回归测试报告已产出（单测 + 集成 + E2E + 视觉）
- [ ] 回滚方案已准备
- [ ] 索引 / README / 版本历史已联动更新

审核内容：
- [ ] 变更必要性（是否有更小改动方案）
- [ ] 变更准确性
- [ ] 影响评估完整性
- [ ] 回归验证充分性
- [ ] 回滚方案可行性
- [ ] 文档同步一致性

审核结果：
✅ 通过 → 提交推送
🔄 需调整 → 返回步骤 3-4 调整后重新审核
❌ 不通过 → 返回步骤 1 重新分析变更
⚠️ 范围溢出 → 返回步骤 0 重新分级或拆分
```

**XL 级别**：
```
🚫 禁止走变更流
必须拆分为多个 M/L 变更，或转工作流 2（规划）重新设计方案
```

---

### 🚨 全局强制规则（不可违背）

1. **先选流再开工** —— 禁止"边做边选"，首次回复必须声明工作流
2. **一个任务一个流** —— 混合任务必须先拆分
3. **产出物对照白名单** —— 出现白名单外的产出 → 立即停工
4. **新文件 = 需求/开发流** —— 变更流永远不新增文件
5. **验证流只读** —— 验证流发现缺陷就停工转交，不在验证流里修
6. **每次变更必须推送** - 不允许本地积压
7. **新增文档必须更新索引** - 索引页 + README
8. **保持视觉风格一致** - 使用统一 CSS 变量
9. **中文优先** - 所有文档使用简体中文
10. **版本号管理** - 每个文档独立版本号
11. **增量提交** - 大任务分多次提交，便于追溯
12. **开发进度同步** - 每个 WBS 任务完成后更新 `开发进度.md`（任务状态 + 提交 ID + 验证结果）

---

### ⚠️ 越界反例（禁止照此执行）

| 反例场景 | 错在哪里 | 正确做法 |
|---------|---------|---------|
| "我一边写 PRD 一边把接口实现了" | 需求流 + 开发流混合 | 拆成两步：先工作流 1 出 PRD，再工作流 3 写代码 |
| "改 Bug 时顺手加了个新特性" | 变更流夹带新功能 | 拆成两步：工作流 5 修 Bug，工作流 3 加新特性 |
| "验证时发现少个页面，顺手写了" | 验证流新增文件 | 停工，转工作流 1 补页面，再重入验证流 |
| "这个改动挺小，直接改了" | 未声明工作流 | 再小的改动也必须先声明走哪个流 |
| "在变更流里新建了一个 helper 文件" | 变更流新增文件 | 新增文件需走工作流 3（开发），或评估是否真需要新文件 |
