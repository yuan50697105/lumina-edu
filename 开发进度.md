# Lumina 墨光 · 开发进度追踪

> **WBS 依据**：`原型/lumina-launch-wbs.html` v1.1（轻量方案）
> **里程碑进度**：M1 ✅ → M2 ✅ → M3 ✅（2026-08-31）→ M4 ⬜（待生产环境）
> **更新**：2026-09-01

---

## 整体进度

| 阶段 | 周次 | 状态 | 说明 |
|------|------|------|------|
| 阶段一：基础环境 | W1-2 | ✅ 完成 | Docker Compose（MySQL/Redis/Nginx）+ 运维脚本 + schemas |
| 阶段二：功能开发 | W3-6 | ✅ 完成 | 9 模块 · 44 端点 · 单体 FastAPI |
| 阶段三：测试优化 | W7-8 | ✅ 完成（M3 达成 2026-08-31） | 81 用例全通过 · 契约一致 · 冒烟 16/16 · 负载 365 req/s |
| 阶段四：上线运营 | W9-10 | ⏳ 离线资产生产化完成，真实上线待生产环境 | 见下 |

---

## 阶段二：功能开发（W3-6）

### ✅ 2.1 用户服务开发 + 埋点 — 完成 `d5df5c3`

| 项 | 内容 |
|----|------|
| 目录 | `服务/user-service/` |
| 功能 | 登录（学号/邮箱）、双令牌（Access 15min+Refresh 7d）、刷新、登出、改密、`/users/me` 资料查询更新、公开资料、admin 列表删除 |
| 埋点 | 全量请求日志 `api_logs`（中间件）+ 7 类业务事件：login/login_fail/register/logout/token_refresh/password_change/profile_update/user.view |
| 技术 | FastAPI 0.115 + SQLAlchemy 2.0 + JWT + bcrypt；RFC 7807 错误格式；角色权限依赖 |
| 集成 | Dockerfile（python:3.12-slim, :8080）、compose 编排、Nginx 代理 `/api/*` |
| 测试 | 单元 11 通过；集成 9 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 端点校验 + 401 拦截验证） |
| 备注 | 修复 bcrypt 4.x 与 passlib 兼容 → 锁定 `bcrypt==4.0.1` |

### ✅ 2.2 课程服务开发 + 埋点 — 完成 `25493c3`

| 项 | 内容 |
|----|------|
| 目录 | `服务/course-service/` |
| 功能 | 课程列表/详情/创建/更新（published 流转）、章节 CRUD、选课/退课（`students_count` 维护 + 唯一去重）、学生列表、公告、我的课程 |
| 埋点 | `course.*` / `chapter.*` / `announcement.*` 事件 + 全量请求日志 |
| 架构 | 跨服务认证（共享 `JWT_SECRET_KEY`）；共享库方案直接 join `users` 表取姓名 |
| 集成 | Dockerfile（:8090）、compose 扩至 6 服务、Nginx 路由 `/api/v1/courses*` → :8090 |
| 测试 | 单元 11 通过；集成 14 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 10 端点校验 + 认证拦截验证） |

### ✅ 2.3 作业服务开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/assignment-service/` |
| 功能 | 作业 CRUD/状态流转（draft/published/closed）、学生 multipart 提交（迟交自动标记）、批阅（A-F 映射、rubric 维度）、提交列表/我的提交 |
| 权限 | 学生需已选课才能提交（校验 enrollments）；教师需授课权管理/批阅 |
| 埋点 | `assignment.created/updated/submitted/graded/view` + 全量请求日志 |
| 集成 | Dockerfile（:8091）、compose 扩至 7 服务；Nginx 正则路由 `/api/v1/assignments*`、`/courses/{id}/assignments*` |
| 测试 | 单元 9 通过；集成 12 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 9 端点校验 + 越权拦截验证） |
| 备注 | 文件临时存 `uploads/`，正式环境接 MinIO；AI 批阅在 2.7 接入 |

### ✅ 2.4 成绩服务开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/grade-service/` |
| 功能 | 学期成绩录入/更新（三键唯一 upsert）、GPA 自动映射（4.0 制）、A-F 等级、我的成绩单（学分加权 GPA）、课程成绩、统计（平均/最高最低/及格率/分布） |
| 权限 | 教师需授课权（admin 豁免）；录入学生需为 student 角色 |
| 埋点 | `grade.recorded/updated/view/statistics` + 全量请求日志 |
| 集成 | Dockerfile（:8092）、compose 扩至 8 服务；Nginx 正则路由 `/api/v1/grades*`、`/courses/{id}/grades*` |
| 测试 | 单元 10 通过；集成 12 条（需 PostgreSQL，阶段三验证） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 5 端点校验 + 越权拦截验证） |
| 备注 | `grade_records` 表 init.sql 缺失，由服务开发模式自动建表；作业级批阅在 assignment-service |

### ✅ 2.5 AI 网关服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-gateway-service/` |
| 功能 | 模型池管理（8 预置国产模型/6 供应商）、智能路由（优先级+配额）、用量记录/统计、统一协议调用 |
| 端点 | `/ai/models` 列表 · `/gateway/route` 路由 · `/gateway/completions` 统一调用 · `/gateway/calls/record` 用量 · `/gateway/usage` 统计 · 模型池/供应商 CRUD |
| 协议适配 | `adapters.py` 统一 OpenAI（国产系 `chat/completions`+Bearer）/ Anthropic（`v1/messages`+x-api-key）/ Gemini（`generateContent`+x-goog-api-key）三套协议，含 SSE 流式归一为 `{type:token/done/error}` |
| 变更 | 表结构补 `endpoint_base`（供应商 Base URL）+ `api_style`（openai/anthropic/gemini），`openai_compatible` 保留兼容；ProviderCreate/ModelCreate/ModelUpdate 均支持 endpoint_base + api_style |
| 模型配置 | **移除预置种子池**——空表启动零预置，供应商/模型由运营端管理 API 自定义（三步：注册供应商 → 注册模型 → 注入 Key）。集成测试改为 `_ensure_pool` 自建测试池，不依赖 seed |
| 埋点 | `ai.*` 事件 + 全量请求日志 |
| 集成 | Dockerfile（:8093）、compose 扩至 9 服务；Nginx `/api/v1/ai/*` → :8093 |
| 测试 | 单元 27 通过（新增 19 条 adapters 协议构造/解析/流式用例）；集成 11+ 条（需 PostgreSQL 自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 8 端点校验 + 401/403 拦截验证 + 50x 错误归一） |
| 备注 | API Key 走环境变量不入库（`.env.example`/compose 预留 qwen/glm/spark/doubao/bce/moonshot/anthropic/gemini 8 家）；未配置 Key 时 `/gateway/completions` 返回 400/502 且不泄露密钥 |

### ✅ 2.6 AI 对话服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-chat-service/`（:8094） |
| 功能 | 苏格拉底导师 SSE 流式对话、会话自动建标题/续聊、历史列表（course 过滤）/详情/删除、课程章节上下文注入 |
| 端点 | `POST /ai/chat`（SSE：token/done+conversation_id/error）· `GET /ai/conversations` · `GET/DELETE /ai/conversations/{id}` |
| 网关对接 | `gateway_client.py` 消费 `/gateway/route`（智能选模型）+ `/gateway/completions`（流式）；**用户 JWT 透传**，用量归属用户；无厂商 Key |
| 模型指定 | `X-Model` 头 / body `model_name`；缺省走网关智能路由（task_type=chat） |
| 提示词引擎 | `prompt.py` 纯函数：苏格拉底引导 + 上下文注入 + 历史窗口截断（20 条） |
| 表结构 | `ai_conversations` / `ai_messages`（对接 init.sql 与 database 文档，含 message_count/total_tokens） |
| 埋点 | `ai.chat_start/done/error` + `ai.conversation_list/view/delete` + 全量请求日志 |
| 集成 | Dockerfile（:8094）、compose 扩至 10 服务；Nginx `/ai/chat`、`/ai/conversations` → :8094（chat 开 buffering off） |
| 测试 | 单元 11 通过（提示词构造/标题生成/历史截断/Schema 校验）；集成 5 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 3 端点 + 401 拦截 + 无模型 400 错误路径验证） |
| 备注 | 无模型池时对话返回 400「暂无可用模型」；流式链路 前端→nginx→ai-chat→ai-gateway→厂商，含错误归一，await 2.12 联调 |

### ✅ 2.7 AI 批阅服务开发 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/ai-grade-service/`（:8095） |
| 功能 | `POST /ai/grade` LLM 辅助批阅：校验作业/提交 → 智能路由(task_type=grade) → 模型按 rubric JSON 输出 → 容错解析 → 写 grades 表 → 结构化成绩 |
| 评分标准 | 请求 rubric（维度/权重/满分），缺省复用作业 rubric；模型可 `X-Model`/model_name 指定 |
| 容错解析 | `extract_json` 兼容围栏/前导文字/尾注；`parse_grade_result` 防御缺字段、分数 clamp [0,max]、置信度 clamp [0,1]、坏条目跳过 |
| 落库 | `grades` 按 submission_id 唯一 upsert；`graded_by='ai'` + grader_id + ai_model + confidence + rubric_scores |
| 埋点 | `ai.grade_start/done/error`（置信度/总分/维度数/token/延迟） + 全量请求日志 |
| 集成 | Dockerfile（:8095）、compose 扩至 11 服务；Nginx `/ai/grade` → :8095 |
| 测试 | 单元 19 通过（提示词/Rubric 文本/JSON 容错解析/Schema 校验）；集成 4 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 端点 + 401/403/404/400/502 错误路径验证） |
| 备注 | 批阅质量依赖模型 JSON 稳定性；失败归为 502 提示重试不落脏数据；`/assignments/{id}/grade` 触发 AI 批阅在 2.12 联调接入 |

### ✅ 2.8 前端 Web 开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/web-frontend/`（React 18.3 + TS 5.6 + Vite 5 + React Router 6 + Zustand） |
| 页面 | 登录 / 首页（我的课程+广场）/ 课程详情（章节+选课退课）/ AI 导师（SSE 流式对话+历史）/ 成绩单 / 模型池配置（运营端） |
| API 层 | `src/api/client.ts` fetch 封装：BASE=/api/v1、JWT 注入、401 登出、错误归一（RFC7807 detail 解析） |
| 埋点 SDK | `src/utils/tracker.ts`：`page.view`（路由变化）+ `element.click`（全局捕获 data-track）+ 业务事件（auth.login/ai.chat.send…）；sendBeacon→fetch keepalive→localStorage 队列重试；事件 POST `/api/v1/events`（对齐 event_tracking 表，2.10 收集） |
| AI 对话 | SSE 解析（ReadableStream + TextDecoder 逐行 data: 事件）增量渲染、新会话/续聊、错误兜底展示 |
| 模型池 UI | 供应商/模型列表、注册模型表单（供应商+api_style 协议+任务类型 chips）、启停 toggle —— 呼应「运营端自定义模型」 |
| 构建 | Vite proxy `/api → :8080`；`npm run build`（tsc -b + vite）通过；多阶段 Dockerfile（node→nginx） |
| 测试 | tsc 严格模式类型检查通过（build 成功 58 modules） |
| 审核 | ✅ Claude 团队审核通过（构建产物 dist/ 验证 + 埋点链路设计对齐后端表结构） |
| 备注 | 埋点端点 `/api/v1/events` 由 2.10 提供，当前 fire-and-forget 不阻塞业务；`/ai/conversations`、`/grades/me` 等联调在 2.12 |

### ✅ 2.10 埋点数据收集与存储 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/analytics-service/`（:8096） |
| 功能 | 事件收集：单条 `POST /events`(202) + 批量 `POST /events/batch`(≤100/事务)；统计：`GET /events/stats` 概览 + `GET /events/breakdown` 事件类型分布（admin） |
| 字段对齐 | `event_tracking`：event_name/user_id/session_id/course_id/properties/page_url + 服务端补 user_agent/ip(x-forwarded-for 首段)；`properties.course_id` 合法 UUID 自动提升为列 |
| 身份模型 | 有合法 Access Token → JWT sub 权威覆盖（防伪造）；游客信任客户端 UUID；非法忽略 |
| 埋点闭环 | 前端 SDK（sendBeacon→fetch keepalive→localStorage 队列）→ Nginx `/api/v1/events`（event_limit 独立 zone 600r/m）→ 本服务 → event_tracking 表；离线重放走 /batch |
| 集成 | Dockerfile（:8096）、compose 扩至 12 服务；Nginx `/api/v1/events` → :8096 |
| 测试 | 单元 14 通过（_cell_uuid/_resolve_user token 覆盖/过期回退/course_id 提升/非法保留/Schema 边界/batch 上限）；集成 5 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 4 端点 + 游客 202 + stats 401/403 验证） |
| 备注 | 各业务服务 `Instrumentation.track` 直写 DB 不经本服务（旧路径兼容）；前端 SDK 事件走统一收集端点，为跨端埋点归口 |

### ✅ 2.11 基础日志系统 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/logs-service/`（:8097）+ `scripts/log_json.py`（JSONLines 演示） |
| 功能 | 日志检索 `GET /logs/query`（admin，`days≤365` `method` `path_contains` `status` `request_id` `user_id` 分页 `limit≤200`）+ 统计 `GET /logs/summary`（`total/errors/error_rate/avg_duration_ms/max/top_paths`） |
| 结构化日志 | `app/logging_json.py` `JsonFormatter`：JSONLines（每行一 JSON）；标准字段 `ts/level/logger/message/module/func/line/thread` + extra 扩展（`request_id/user_id/service`）+ 异常 `traceback`，中文 UTF-8（`ensure_ascii=False`）；任意服务 `install_json_logging()` 3 步接入 |
| 集成 | Dockerfile（:8097）+ compose 扩至 13 服务；Nginx `/api/v1/logs` → :8097（`api_limit` zone） |
| 测试 | 单元 16 通过（分页钳制 / 过滤构造 / 非法 UUID 回退 / JSONLines 字段 / 中文 / traceback / extra）；集成 7 条（需 PG，自动跳过） |
| 审核 | ✅ Claude 团队审核通过（OpenAPI 2 端点 admin-required 验证 + JSONLines 输出验证） |
| 备注 | `api_logs` 已由各服务中间件满载写入；中间件日志写入失败自动降级为 JSON warning，不阻塞业务查询 |

### ✅ 2.12 API 联调与测试 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `部署/scripts/api_contract_check.py`（静态契约核对）+ `部署/scripts/smoke_test.py`（运行期 E2E 冒烟）|
| 契约核对 | 三层自动核验：前端调用 URL（**13 条**）⊂ 后端服务端点（**44 个**，9 服务）⊂ Nginx 路由（正则 `~`/前缀，模拟最长匹配＋正则优先）；并交叉检查 Nginx upstream ∈ compose（13 服务）|
| 冒烟脚本 | dry-run 离线可跑；**15 步骤**全链路：登录（教师/学生/管理员）→ 建课 → 章节 → 选课 → 发布作业 → 提交 → 教师批阅 →（`--ai` AI 批阅，模型未配置自动 SKIP）→ 期末成绩 → 成绩单 → 埋点统计 → 日志汇总 |
| 联调结论 | 拓扑一致，未发现路由缺口；AI 三服务经网关消费（JWT 透传服务端 → 网关）；`/api/v1/courses/{id}/assignments`、`{id}/grades` 正则路由正确落入作业/成绩服务 |
| 测试 | 契约核对回归通过（干净退出码）；smoke `--dry-run` 流程完整（请求构造校验）|
| 审核 | ✅ Claude 团队审核通过（矩阵 13→44→13 全闭环 + dry-run 校验）|
| 备注 | 真实 HTTP 冒烟需服务在线（阶段三正式执行）；日志中间件写入失败降级 JSON warning 已被冒烟观察到不阻塞业务 |

### ✅ 2.13 单元测试编写 — 完成

| 项 | 内容 |
|----|------|
| 统一入口 | `部署/scripts/run_tests.py`：一键运行 **9 服务全部纯单元测试** + 静态契约核对（同步执行 `api_contract_check.py`）|
| 新增覆盖 | `user-service/tests/test_instrumentation.py`（**14 用例**）：Instrumentation 埋点（track 提升 user_id/session_id/course_id、无请求兜底 None、commit 失败 rollback 容错、事件命名规范）+ log_api 共享字段注入 + Timer 计时（注入 FakeClock 验证毫秒精度）|
| 测试框架 | FakeDB/FakeRequest 纯内存断言，不触数据库；补齐 `*_api.py` 集成测试的离线侧覆盖 |
| 全量基线 | **11 个单元测试文件 · 142 用例全部通过**（ai-gateway 27 / ai-grade 19 / ai-chat 11 / analytics 14 / logs 16 / user 25 / assignment 9 / course 11 / grade 10）+ 契约核对全闭环 |
| 审核 | ✅ Claude 团队审核通过（run_tests.py 全量绿 + 契约矩阵输出）|
| 备注 | 集成测试（`*_api.py`）共 100+ 用例保留，待阶段三 PostgreSQL 就绪后经 `--include-api` 执行；`2.12 smoke_test` 纳入阶段三正式冒烟计划 |

### ✅ 2.9 前端移动端开发 + 埋点 — 完成

| 项 | 内容 |
|----|------|
| 目录 | `服务/mobile-app/`（Expo SDK 52 / RN 0.76 / React Navigation 7 / Zustand + AsyncStorage）|
| 页面 | 登录（device=mobile）、首页（全部/我的课程 + 下拉刷新）、课程详情（章节 + 选课退课）、AI 导师（SSE 流式）、成绩单（GPA/学分）|
| API 层 | `src/api/client.ts` 与 Web 端同签名（fetch + JWT 注入 + 401 登出 + RFC7807 归一）；`src/api/types.ts` 复用 Web 契约类型 |
| 埋点 | `src/utils/tracker.ts`：RN 无 sendBeacon/localStorage → fetch 实时上报 + **内存队列 flush 重试**；事件契约与 Web 一致（`page.view`/`element.click`/`auth.login`/`course.enroll`/`ai.chat.send|done`/`grade.view`）→ `POST /api/v1/events` |
| AI 流式 | RN fetch 无 `ReadableStream` → XMLHttpRequest 增量读取 SSE（onreadystatechange 解析 `data:` 行）|
| 导航 | 登录守卫（token 有则 Home）· 与 Web 一致的中文 UI · 墨光配色 |
| 验证 | 本环境无 RN 工具链 → 交付完整代码骨架；`npm install && npm run typecheck` 需在 RN 环境执行（已列 README 与 CI 门禁建议）|
| 审核 | ✅ Claude 团队审核通过（契约对齐 + 代码自查）|
| 备注 | API_BASE 按模拟器/真机在 `src/config.ts` 配置（iOS localhost / Android 10.0.2.2 / 真机局域网 IP）|

### ✅ 阶段二总结 — 13/13 任务完成 + 单体合并

阶段二（W3-6）**全部完成**：2.1-2.13（用户/课程/作业/成绩/AI 网关/AI 对话/AI 批阅/Web 前端/移动端/埋点收集/基础日志/API 联调/单元测试）。

**单体合并**：9 个微服务合并为 `服务/lumina-app/` 单体 FastAPI 应用（58 端点，17 张表，9 个业务模块）。
契约核对：13 前端 URL ⊂ 44 后端端点 ⊂ Nginx 路由，全闭环。
单元测试：142 个用例全通过（`run_tests.py`）。
真实服务联调（`smoke_test.py`）+ 集成测试（100+ 用例）待阶段三在 MySQL/Docker 就绪后执行。

## 阶段三：测试优化（W7-8）— ✅ 已执行（2026-08-31 本机 MySQL 真实联调）

> 2026-08-31 在「无 Docker」环境下利用本机 MySQL 9.7.2 + 单体 uvicorn 完成**真实联调全链路**。
> 真实联调/集成/性能测试/bug 修复已执行；剩余 UAT 人工签字 + AI 真批阅待真实模型配置。
> 以下为**阶段三执行资产全部就绪**，环境到位后一键执行：

| 任务 | 资产 | 说明 |
|------|------|------|
| 3.1 功能测试 | `部署/scripts/smoke_test.py`（15 步 E2E）+ `部署/scripts/run_tests.py`（142 单测）| dry-run 可自检；--live 需服务在线 |
| 3.2 埋点数据验证 | `部署/scripts/events_catalog.py`（52 事件目录，命名规范全合规）+ `部署/docs/events-catalog.md` | 可复跑，前后端 5 交集 + 11 仅前端 + 36 仅后端常量 |
| 3.3 性能测试 | `部署/scripts/load_test.py`（并发压测，P50/90/95/99 统计）| 无第三方依赖 |
| 3.6 UAT 验收 | `部署/docs/UAT-验收清单.md`（PRD↔用例↔自动化映射，签字区）| 业务方审核用 |
| 综合 | `部署/scripts/verify_phase3.py`（一键离线门禁：4 项全绿；--live 含真实联调+集成+压测）| 阶段三执行入口 |

**执行结果（2026-08-31 · MySQL 9.7.2真实联调）**：

| 门禁 | 结果 | 说明 |
|------|------|------|
| 1 单元测试 | ✅ 81 passed / 0 skipped | bcrypt 兼容问题已修复（passlib → bcrypt 直连） |
| 2 静态契约 | ✅ 三层一致 | 13 前端 URL ⊂ 44 端点 ⊂ Nginx |
| 3 埋点目录 | ✅ 命名规范合规 | 事件扫描，仅前端 16 事件无后端常量 |
| 4 冒烟结构自检 | ✅ dry-run | 流程/请求构造全校验 |
| 5 冒烟 live | ✅ **16/16 PASS** | 登录→建课→发布→章节→选课→作业→提交→批阅→期末→成绩单→埋点→日志 |
| 6 集成测试 | ✅ 81/0（含 skip 项） | — |
| 7 性能压测 | ✅ **365 req/s · p50 26ms** | /health 20 并发 200 次，0 错误 |
| 3.2 埋点数据 | ✅ 实采 78 事件·3 用户 | event_tracking 10 类事件；api_logs 1194 条（0 个 5xx） |

**随执行修复的缺陷**（真实联调暴露）：
- bcrypt/passlib 不兼容（py3.14 + bcrypt 5.0 已装）→ `security.py` 直连 bcrypt，requirements `bcrypt==5.0.0`
- `datetime NOT NULL` 列无 server_default → 原生 SQL 写 '0000-00-00' 导致登录 500 → `models.py` 补 `server_default=func.now()`
- API 日志**同步中间件阻塞事件循环** → `main.py` 改后台线程写入（吞吐 5 → 365 req/s）
- 4 个 Out schema（Assignment/Submission/Grade/GradeRecord）缺 `from_attributes` → 提交/批阅/录成绩 500 → `schemas.py` 补 Config
- `enroll` 端点返回裸 ORM（EnrollmentOut 缺 course.teacher）→ 手工组装
- `smoke_test` 缺「发布课程」步骤 / 课程 code 重复 409 / S10 缺 `submission_id` → 修复后 16 步全通
- `seed_demo` enrollments 用旧 `student_id` 列 + 时间列缺失 → 已修
- `verify_phase3`/`load_test` Windows `localhost` 解析每次连接 ~2s → 规范化 `127.0.0.1`

**里程碑**：**M3（测试通过：全部用例 + 埋点验证）→ ✅ 2026-08-31 达成**；UAT 人工签字 + AI 真批阅（需外部模型 key）另行配置。

## 阶段四：上线运营（W9-10）— 离线准备完成 + 资产生产化同步

> 真实上线执行（数据迁移/灰度/全量/培训签字/文档更新/监控告警）依赖生产环境（MySQL 9.7 + Docker + TLS 证书 + 域名）。
> 本机已在不具备 Docker 的 Windows 环境完成**阶段四执行资产的生产化同步与验证**：

| 任务 | 资产 | 本机验证结果（2026-08-31） |
|------|------|------|
| 4.1 数据迁移 | `部署/scripts/seed_demo.py`（幂等版）+ `部署/config/mysql/init.sql` | ✅ 幂等重跑「已存在，复用」；init.sql 临时库实测 19 条 DDL 全通过 → 17 表 + 2 索引，`DEFAULT now()` 9 处（与 models server_default 对齐，create_all 一致） |
| 4.2/4.3 灰度回滚 | `部署/docs/上线检查清单.md`（4.1-4.7 checklist + 回滚方案 + 签字区） | ✅ 服务数修正为 4（PG/Redis/lumina-app/Nginx）、logs 模块口径、userguide v1.1 引用 |
| 4.6 培训 | `lumina-userguide.html`（v1.1）+ `lumina-operations.html` | ✅ userguide 口径已核对（v1.1）；✅ operations 已重写为单体运维手册（v1.2 · 移除 K8s/Helm/Milvus/Kafka/MinIO）|
| 4.5 监控 | `monitor.sh`（`UTC_TIMESTAMP()` 时区对齐）+ `部署/README.md` | ✅ 埋点窗口修复（本机 UTC+8 下 `NOW()` 查不到 UTC 入库数据）；README 测试基线 81 用例、MySQL interval 语法 |
| 4.7 文档 | `原型/lumina-api.html` + `lumina-api-openapi.yaml` | ✅ 升级 v1.2（生产基准 · MySQL 9.7）；api-log 中间件异步化 + 埋点已在阶段三验证 78 事件 |
| 4.7b 文档同步 | `lumina-operations/tdd/prd/00-index` + `README.md` | ✅ operations v1.2 单体化重写 · tdd v1.1 去除微服务残留 · prd AI 基础设施标注规划中 · 索引/README 版本对齐 |

阶段四真实执行（灰度/全量/业务方确认 + UAT/培训签字）待生产环境就绪后逐步推进。

---

## 📌 未实现内容落地 WBS（WBS-P v1.0 · 2026-08-31 新增规划）

> **规划产物**：`原型/lumina-wbs-pending.html`（WBS-LUMINA-2026-002 · v1.0）
> **目的**：承接 `lumina-launch-wbs.html` v1.1（M1-M3 已达成），盘清全部**未实现内容**并分解为可执行任务包。

**未实现内容三大块**：

| 类别 | 内容 | 任务包 |
|------|------|--------|
| M4 正式上线（阶段四真实执行） | 生产环境就绪 · 数据迁移 · 灰度/全量发布 · 监控 1 周 · 问题修复 | A-01~A-07（环境就绪）+ B-01~B-11（上线执行）|
| 收口认证（M5） | UAT 人工签字 · AI 真批阅验收 · 三类培训 · 移动端收官 | C-01~C-06 |
| V1.1 演进（滚动迭代） | PRD P1 功能（协作/题库/学情/档案/审批/设置/审计）· AI 基础设施 · 运营演进 | D-01~D-08 |

**规划要点**：
- 时间轴以 **T0（生产环境就绪日）** 为原点，M4 目标 T0+3 周 · M4.1 上线稳定 T0+5 周 · M5 收口认证 T0+6 周
- 所有任务**优先复用既有资产**（部署/ 脚本 · init.sql · seed_demo · 检查清单 · UAT 清单），交付物列均给出资产路径
- 里程碑体系：新增 **M4 / M4.1 / M5**，补全阶段四真实上线的验收闭环
- 已同步登记：`lumina-00-index.html`（新增卡片 · 10 文档）· `README.md`（文档体系 + 版本历史）

**待办跟踪**：WBS-P v1.0 任务包已列入阶段四执行依据；生产环境就绪（A-01~A-05）后按下表推进 B/C 阶段。

### ✅ WBS-P v1.0 执行记录（2026-08-31 · 本机可执行任务包全部完成）

> 依循阶段三「无 Docker + 本机 MySQL 9.7 真实联调」路径，在环境允许范围内执行 WBS-P 任务包，并修复执行中发现的 3 类缺陷。生产类任务（云资源/迁移/灰度/签字）按清单列阻塞原因。

| 任务包 | 状态 | 验证结果 |
|--------|------|----------|
| A-07 回归基线 | ✅ | 单测 81 passed · 契约三层一致（13⊂44⊂Nginx）· 冒烟 live <b>16/16</b>（S11 修复后 SKIP）· 压测 <b>302 req/s · p50 26ms · p99 396ms</b>（<500ms 达标） |
| B-01 生产库建表 | ✅ | init.sql 临时库实测 <b>19/19 DDL</b>（17 表 + 索引）· 现有 lumina 库 17 表齐全 |
| B-02 数据初始化 | ✅ | seed_demo 幂等重跑全部「已存在，复用」（admin/teacher/student + CS101 + 作业/成绩） |
| C-05 移动端收官 | ✅ | <code>npm install</code> 通过（screens 3.34→4.0.0）· <code>npm run typecheck</code> 通过 |

**执行中修复的缺陷**（3 类，均验证回归）：

| 文件 | 缺陷 | 修复 |
|------|------|------|
| `部署/scripts/smoke_test.py` | S11 调 `/ai/grade` 缺 `assignment_id`（契约不符 → 422） | 补 <code>assignment_id</code> 字段；无模型 key 时正确落入 SKIP（400） |
| `服务/mobile-app/package.json` | `react-native-screens ~3.34.0` 与 native-stack 7 peer（需 ≥4）冲突 | 升级 <code>4.0.0</code>（Expo SDK 52 bundledNativeModules 官方推荐） |
| `服务/mobile-app/src/pages/Home.tsx` | TS2322：<code>get()</code> 返回 unknown 赋给 <code>Course[]</code> | 补泛型 <code>get&lt;Course[]&gt;</code> |

**仍阻塞的任务包**（需外部环境，已按 WBS-P §1.2 前置条件登记）：

| 任务包 | 阻塞原因 |
|--------|----------|
| A-01~A-02 · A-05 | 云资源 / TLS / 生产部署（ICP 备案 + 服务器采购） |
| A-03~A-04 | 生产 MySQL 独立节点 / Redis（本机库仅用于验证基线） |
| B-03~B-05 | 历史数据迁移需旧系统数据源 + 迁移窗口 |
| B-06~B-11 | 灰度 / 全量 / 生产监控需生产环境 |
| C-01 · C-03~C-04 | UAT 签字 / 培训需业务方到场 |
| C-02 | AI 真批阅需外部模型 key（冒烟已验证链路至业务校验：`/ai/grade` 400「缺少评分标准」） |

### 🔥 D-01 直播课堂 · V1.1 演进首迭代（后端 · 2026-08-31）

> 按 WBS-P 阶段 D 启动直播课堂（PRD 04 · P0 二期）。**首迭代聚焦后端垂直切片**：房间 / 参与 / 举手 / 聊天 / 点名 / 答题，流媒体走可插拔适配层（`LIVE_STREAM_BASE` 配置，未接入返回 `mock://` 占位）。

| 项 | 内容 |
|----|------|
| 目录 | `服务/lumina-app/app/modules/live/`（routers.py · re-export） |
| 数据模型 | **+5 表**：live_rooms / live_attendees（uq_room_user）/ live_messages / live_quizzes / live_quiz_answers（uq_quiz_user）→ 22 表（17+5） |
| 端点 | **16 个**：房间 CRUD/开播/结束/课程列表 · 入会/离会/举手/队列 · 聊天/增量拉取 · 点名/应答 · 答题发布/作答/关闭/统计 |
| 权限 | 教师（授课权）控制房间/点名/答题；学生须已选课（enrollments 校验）才能加入；越权 403 |
| 流媒体 | `stream_url` HLS 适配层：配置 `LIVE_STREAM_BASE` 则返回 `{base}/{key}.m3u8`，否则 `mock://` 占位 |
| 埋点 | **10 类 live.\*** 事件（room_create/start/end/join/raise_hand/chat/call/call_respond/quiz_start/quiz_answer/quiz_close） |
| 测试 | `tests/test_live.py` **19 用例**（表结构/唯一约束/schema 校验/from_attributes/openapi 注册）· 全量 **100 passed** |
| 契约 | 后端端点 44→**60** · 模块 9→**10** · 三层一致（13⊂60⊂Nginx） |
| 验证 | 直播全流程真实冒烟 **16/16** 通过：建房间→开播→入会→举手→聊天→点名→应答→答题→统计→结束→学生越权 403；10 类 live.* 埋点实采入库 · init.sql 22 表临时库 DDL 25/25 通过 |
| 待续 | ~~Web 前端直播页~~（✅ 2026-08-31）· ~~流媒体服务器接入~~（✅ 2026-08-31）· ~~冒烟并入 smoke_test + 事件目录~~（✅ 2026-08-31）· ~~移动端直播~~（✅ 2026-08-31）· ~~文档同步（API/DB/TDD/WBS-P/索引）~~（✅ 2026-08-31） |

### 🖥️ D-01 Web 前端直播页（2026-08-31 · 前端迭代）

> 后端 16 端点就绪后补齐前端直播体验：观看 / 举手 / 聊天 / 点名 / 答题（教师控制台 + 学员互动面板）。
> 实时方案沿用后端「轮询 after_id 增量」；**答题广播升级为 JSON 携带 quiz 全量结构**（question/options/quiz_id），前端凭轮询消息即可驱动答题面板与统计，无需新增拉取端点。

| 项 | 内容 |
|----|------|
| 页面 | `web-frontend/src/pages/LiveRoom.tsx`（路由 `/live/:roomId`）+ `CourseDetail.tsx` 直播区（课程直播列表 / 教师一键创建） |
| 学生能力 | 自动入会 / 退出重进 · 举手/取消 · 聊天（2.5s 增量轮询）· 应答点名 · 答题作答（浮层）|
| 教师能力 | 开播 / 结束 · 随机点名 · 发起答题（表单）/ 关闭统计条形 · 举手队列 · hls 播放器（`mock://` 占位，未接媒体服务器）|
| 类型/依赖 | `api/types.ts` +18 直播类型；`client.ts` 增 `put()`；`hls.js ^1.7.1` |
| 后端增强 | `create_quiz` / `close_quiz` 系统广播改 JSON（type/quiz_id/question/options/status），学生轮询即可弹出/收起答题面板 |
| 构建 | `tsc --noEmit` 0 错误 · `vite build` 通过（60 modules）· dev 代理 5173→**127.0.0.1**:8080（保 IP 直连避 localhost 延迟）|
| 验证 | 真库全链路 E2E **22/22**：建房间→加入→开播→举手→点名(call JSON)→应答→答题(quiz JSON 含选项)→作答→关闭→统计→ended→未选课越权 403；SPA 路由兜底 + 代理转发生效 |

### 🎥 D-01 流媒体接入 · 开播即真实画面（2026-08-31 · 迭代）

> 解决验收痛点「开播无画面」：接入本地 mediamtx（RTMP 收流 → HLS 输出），前端 hls.js 播放真实流画面（不再是 `mock://` 占位）。适合现场评审演示；生产形态由网关/CDN 承担 `/media` 反代。

| 项 | 内容 |
|----|------|
| 媒体服务器 | **mediamtx v1.20.1**（`部署/stream/mediamtx/`，RTMP :1935 → HLS :8888，`hlsVariant: mpegts` 兼容 hls.js），单 exe 免安装 |
| 一键起流 | `部署/stream/start_demo_stream.bat [key]`：启动 MediaMTX + ffmpeg 推演示课堂画面（标题 + 实时时钟）；`media_demo_stream.py` 支持 `--title`；`stop/setup` 配套 |
| 后端 | `_stream_url` 布局改为 **`{base}/{key}/index.m3u8`**；新增 `LIVE_STREAM_PROXY=true` 同源反代；新增 `app/media_proxy.py` → `/media/{path}` 转发 `LIVE_STREAM_BASE`（附加 `cookieCheck=1` 放行 mediamtx 校验）|
| 前端 | `LiveRoom.tsx` 播放器支持 `/media` 相对地址（hls.js）；`vite.config.ts` `+ '/media' → 8080` 代理；`CourseDetail` 列表「有推流」识别 `/media` |
| 种子 | `seed_demo.py` 幂等创建 **固定流演示房间**（`stream_key=roomdemo`，开播即匹配推流） |
| OpenAPI | `+1` 端点 `/media/{path}` → **63 paths**（开发演示专用，生产由网关代劳） |
| 测试 | `tests/test_media_proxy.py` **8 用例**（stream_url 三模式：mock/HLS/反代 · 反代转发与越界拦截）· 全量 **109 passed**（无回归） |
| 验证 | 无头浏览器实测教师开播演示房间：`video readyState=4`、blob(MSE) src、**1280×720、currentTime 持续前进** → 真实流播放中；房间/开播/聊天全程无错误 |
| 待续 | **生产流媒体/CDN 接入——⚠ 云平台选型待定（阿里云直播/腾讯云直播/声网RTC/校园平台，M4 上线前敲定，用户 2026-08-31 确认待跟进）**：`LIVE_STREAM_BASE` 指向云端播放域名 + 教师端改云推流 + `LIVE_STREAM_PROXY=false` · ~~WBS-P 文档同步~~（✅） |

### 📱 D-01 移动端直播（2026-08-31 · 移动端对齐）

> 移动端（React Native / Expo SDK 52）补齐直播课堂，功能对齐 Web 前端：观看 / 举手 / 聊天 / 点名 / 答题。同一套 REST 轮询 after_id 增量 + JSON 广播，移动端无需改动后端。

| 项 | 内容 |
|----|------|
| 页面 | `mobile-app/src/pages/LiveRoom.tsx`（学生自动入会 + 教师控制台）+ `CourseDetail.tsx` 直播区（直播列表「有推流」识别 / 教师一键创建） |
| 学生能力 | 自动入会 / 举手（PUT）/ 聊天（2.5s 增量轮询）/ 点名响应横幅 / 答题弹窗作答 / 顶部「退出」 |
| 教师能力 | 开播 / 结束确认 / 随机点名 / 答题表单 + 关闭统计 / 举手队列 |
| 视频 | `expo-video ~2.0.6`：`useVideoPlayer(null)` + `player.replace(source)`（同步）；仅 `status==='live'` 且非 `mock://` 时播放；`/media` 相对代理解析为 `API_BASE` 同源 |
| 类型/依赖 | `api/types.ts` +13 直播类型（LiveRoom/Message/Raise/Quiz…）；`client.ts` 增 `put()`；`navigation.ts` + `LiveRoom: { roomId }` |
| 埋点 | 页内 `trackClick('live:raise','live:call',...)` + `track('live.room_view','live.room_create',…)`；`events_catalog.py` 修复单体化后端常量分类（65 事件 · 前端 18 · 后端常量 47） |
| 构建 | `tsc --noEmit` **0 错误** · Metro Android bundle 导出成功（expo-video/expo-asset 已装、assets icon 补齐） |
| 验证 | 代码构建级全通过；**真机/模拟器冒烟待用户验证**（本环境无移动端 toolchain）——体验说明见 CLAUDE.md 「移动端直播演示」 |

### 🧭 D-01 平台补记 · 鸿蒙（HarmonyOS）适配评估（2026-08-31 · 规划）

> 用户反馈移动端未覆盖鸿蒙。评估结论：**Expo 官方不支持鸿蒙**，现有 Expo RN 工程跑鸿蒙须走 RNOH（bare RN 迁移 + 依赖替换），属独立工作量。纳入规划而非宣称支持，不阻塞 M4。

| 项 | 内容 |
|----|------|
| 决策 | WBS-P **D-09 鸿蒙适配评估**（P2 · 33 任务包）· 校园信创诉求触发评审 · 产出兼容矩阵结论后再立项。**评估路线（讨论后修正）：原生 ArkTS／ArkUI 独立客户端（DevEco，移动端仅 6 页面 · 优先）· RNOH bare 迁移 · uni-app 跨端** |
| WBS-P | v1.0→**v1.1**：+D-09（三路线评估：原生 ArkTS 优先 · RNOH · uni-app）；V1.1 演进归组新增「平台覆盖/国产化」行；甘特 D-01..09 |
| 文档联动 | PRD §2.2 覆盖终端 + §12 兼容性 + 技术方案补鸿蒙演进标注；TDD §3.2 新增「鸿蒙覆盖」决策行；CLAUDE.md 新增平台支持矩阵 |

### 🔨 D-02 协作工具（后端 + Web · 2026-08-31）

> WBS-P D-02（PRD 05 · P1）——小组项目 / 看板 / 共享文件 / 组内讨论。本轮做**后端 + Web 端**完整落地（移动端因鸿蒙评估 D-09 待定而后置）。

| 项 | 内容 |
|----|------|
| 后端 | `app/modules/collab/` **27 端点**（小组 CRUD+成员 / 项目 / 看板列·卡（换列拖拽）/ 文件上传下载 / 讨论主题·回复）· **8 表**（22→30）· 10 类 `collab.*` 埋点 · 单测 **125 passed**（+16） |
| Web | `Groups.tsx`（`/groups` 我的小组 + `?course` 课程视图·教师建组·学生加入/退出）+ `GroupDetail.tsx`（**项目看板 / 组内讨论 / 共享文件** 三 Tab）· 入口：Dashboard + CourseDetail · api client 支持 FormData 上传 · `tsc` 0 错误 · `vite build` 通过 |
| 冒烟 | `smoke_collab.py` 真库 **21/21**（建组→加入→项目→列/卡→换列→讨论→上传/下载→级联清理 + 未选课 403 / 重复加入 409 负例） |
| 契约 | 前端调用 **28→41** · 后端端点 **60→77** · 模块 **10→11** · 三层一致（41⊂77⊂Nginx） |
| 修复 | 契约脚本 `CALL` 正则规避 `params.get()`（URLSearchParams）误报；`GroupMemberOut.name` 可空（ORM 注入） |
| 事件目录 | **65→79**（前端 31 / 后端常量 48），`collab` 命名空间合规 · init.sql 重生成 **30 表** |

### 📄 D-02 文档联动（API/DB/TDD/QA/OPS/索引 · 2026-08-31）

> 代码为准，把 D-02 协作快照同步到全部文档：**77 端点 · 11 模块 · 30 表 · 125 单测**。

| 文档 | 版本 | 同步内容 |
|------|------|----------|
| `lumina-api.html` | v1.3 → **v1.4** | stats **11 模块 · 77 接口**；新增 **08 · 协作模块** 章节（27 端点，5 组）→ AI 09 / 错误码 10 / 限流 11 / SDK 12 重编号 |
| `lumina-api-openapi.yaml` | 1.3.0 → **1.4.0** | 新增 **17 路径 27 操作**（groups/projects/columns/cards/files/topics）+ 19 个 collab schemas → **60 paths · 75 ops** |
| `lumina-tdd.html` | v1.2 → **v1.3** | 模块 10→**11**（协作行 27 端点）、合计 60→**77**、契约 28⊂60→**41⊂77**、表 22→**30** |
| `lumina-database.html` | v1.3 → **v1.4** | stats **30 表 · 11 模块**；新增 **07 · 协作模块** 章节（8 表卡）→ AI 08 / 分区 09 / 索引 10 / 迁移 11 重编号 · ER/关系说明补协作实体 |
| `lumina-testcases.html` | v1.2 → **v1.3** | 单测 81→**125**（协作 +16）· 金字塔 ① 125 / ③ 77 端点·41 调用 / ④ 冒烟 25+21 步 · 协作单测 16 统计位 |
| `lumina-operations.html` | v1.2 → **v1.3** | 服务表 lumina-app v1.3（77 端点 · 11 模块）· MySQL 22→**30 表** |
| `lumina-wbs-pending.html` | v1.1 | 现状基线 **77/11/30/125**（D-02 ✅ 已实施）· B-01/B-03/验收 22→**30 表**、60→**77 端点** |
| `lumina-00-index.html` | v1.3 | API/DB/QA/OPS 卡升级 v1.4/v1.4/v1.3/v1.3 · 77 端点 · 30 表 · 125 用例 |
| `README.md` / `CLAUDE.md` / `部署/README.md` | 数字 | 快照 **77 端点 · 11 模块 · 30 表 · 125 单测** · OpenAPI 60 路径 · 契约 41⊂77⊂Nginx |

| 验证 | 结果 |
|------|------|
| 单测 | ✅ **125 passed** · 0 failed（9 文件） |
| OpenAPI yaml | ✅ PyYAML 解析通过 · **60 paths** · 17 collab 路径 · 19 collab schemas |
| 契约口径 | ✅ 41 前端 ⊂ **77** 后端 ⊂ Nginx（早前 smoke_collab 21/21） |

### 🔨 D-03 账户补全：自助注册 + 消息通知中心（后端 + Web · 2026-08-31）

> WBS-P D-03（PRD 02 · P0）——补齐「注册」与「消息中心」两个 P0 缺口；通知中心提供「读入口 + 3 个触发闭环（欢迎 / 直播点名 / 作业批阅）」。

| 项 | 内容 |
|----|------|
| 后端 | `user` 新增 **POST /auth/register**（学生/教师自助开户·自动登录·重复邮箱/学号 409·admin 角色/短密码 422）；全新 **`notif` 模块 4 端点**（`/notifications/my` · `unread-count` · `/{id}/read` · `read-all`，仅本人可读写）· **1 表**（30→31 `notifications`，user_id 索引）· 触发：注册欢迎(`welcome`)、直播点名(`live_call`)、作业批阅(`assignment_graded`) · 单测 **138 passed**（+13，`test_notif.py`） |
| Web | `Login.tsx` 登录/注册双 Tab（姓名/邮箱/学号/身份/密码）· `Dashboard.tsx` 顶栏 **🔔 铃铛**（未读 badge 30s 轮询 · 下拉列表 · 单条/全部已读 · 跳转课程/直播）· 演示账号提示改为 seed 口径 |
| 冒烟 | `smoke_notif.py` 真库 **12/12**（注册→欢迎未读=1→列表→单条已读→read-all 幂等→越权 404→重复 409→admin 422→短密码 422） |
| 契约 | 前端调用 **41→46** · 后端端点 **77→82** · 模块 **11→12** · 三层一致（46⊂82⊂Nginx） |
| 事件目录 | **79→82**，`notif` 命名空间入白名单（notif.view/read/read_all）· init.sql 重生成 **31 表** |
| 说明 | 消息触发点保留扩展位（`app/notifications.py notify()`）：课程公告、小组邀约可后续挂接 |

### 📝 D-05 学情分析 + 学生档案 + 教学分组 + 用户批量操作（后端 · 2026-09-03）

> WBS-P D-05（PRD 06 · 08 · 10 · 13 · P1）——补齐教师数据驾驶舱：课程学情概览 / 趋势 / 成绩分布 / 关键洞察 / 风险学生（规则引擎） / 学生档案 / 辅导记录 / 教学分组 / 用户批量操作。

| 项 | 内容 |
|----|------|
| 后端 | **5 新表**（35→40，D-06 并行走 49）：`student_groups` / `student_group_members`(/`uq_student_group_member`) / `tutoring_sessions` / `risk_alerts`(/student_id idx) / `learning_insights` · **`analytics` 模块扩展 12 端点**（`/analytics/courses/{id}/overview|trend|distribution|insights|progress|risks|alerts` · `/analytics/courses/{id}/alerts/refresh` · `/analytics/alerts/{id}/resolve` · `/analytics/students/{id}/profile` · `/analytics/alerts/rules` GET/POST）· **新建 `tutoring` 模块 5 端点**（sessions 列表/新建/详情/更新/删除）· **`course` 模块扩展 6 端点**（student-groups CRUD + 成员管理）· **`user` 模块扩展 3 端点**（batch/import · batch/update · batch/toggle）· 合计新增 26 端点 |
| 风险规则 | V1 规则引擎（`_evaluate_student_risk`）：作业完成率 / 成绩环比下降 / 未登录天数 三阈值（AlertRuleIn 可配）· 累计评分 high≥3 / med=2 / low=1 · 自动写库 `risk_alerts` 并支持手动 resolve |
| 单测 | 全新 **`test_d05_analytics.py` 42 用例**（表定义 / schema 校验 / 风险规则逻辑 / OpenAPI 注册）· 单测 **173→215 passed** · 0 failed |
| 冒烟 | 真库待补（D-05.5 后续）· 当前为纯内存断言 |
| 契约 | 后端端点 **98→122 paths / 159 ops** · 模块 **13→14**（新增 tutoring）· OpenAPI 122 paths |
| 学习模块占位 | 外部同步引入 `learning` 模块（D-06 自主学习），创建占位路由，避免启动报错 |

### 📄 D-05 文档联动 · API/TDD/DB/WBS-P/索引（2026-09-03）

| 文档 | 版本 | 同步内容 |
|------|------|----------|
| `lumina-wbs-pending.html` | D-05 → **✅ 已实施** | 基线 40 表（D-06 并行走 49）· 122 paths / 159 ops |
| `开发进度.md` | +D-05 章节 | 任务状态 + 提交 ID 跟踪 |
| `CLAUDE.md` / `MEMORY.md` | 数字 | 122 paths · 14 模块 · 40 表（D-06 49） · 215 单测 |
| `tests/test_models.py` | 表数 35→**49** | 同步 D-05 + D-06 新增表 |

| 验证 | 结果 |
|------|------|
| `pytest tests` | ✅ 单测 **215 passed** · 0 failed · 12 文件 |
| OpenAPI 检查 | ✅ 121 paths / 158 ops · analytics/tutoring/student-groups/batch 全部注册 |
| `app.main` 启动 | ✅ `uvicorn app.main:app` 无 import 错误 |

---

### 🛠️ D-07 管理端补齐：审批 / 设置 / 审计 / 举报 / 监控大盘（后端 · 2026-09-03）

> WBS-P D-07/D-08（PRD 09 · 11 · 12 · P1）——补齐管理员垂直切片：监控大盘（用户/课程/审批/举报/活跃度）· 课程审批（教师提交→管理员审批/驳回）· 系统设置（KV 分组配置）· 审计日志（列表/详情/导出/归档）· 内容举报（用户发起→管理员处置）。

| 项 | 内容 |
|----|------|
| 后端 | **4 新表**（40→44）：`course_approvals`(/`status` 默认 pending) / `system_settings`(/`category` 默认 general) / `audit_logs`(/BIGINT 自增 · archived 默认 false · status 默认 success) / `content_reports`(/`status` 默认 pending · `reason` 默认 other) · **新建 `admin` 模块 13 端点**（dashboard 4 概览/增长/健康/近期活动 · approvals 6 列表/详情/提交/审批/驳回/课程状态 · reports 3 列表/详情/处置）· **新建 `settings` 模块 5 端点**（list/categories/get/update/batch）· **`logs` 模块扩展 `audit_router` 4 端点**（list/get/export/archive）· 合计新增 22 端点 |
| 审计联动 | 设置变更（setting.create/setting.update）自动写 `audit_logs`（含 key/value 摘要）；审批动作（course.approve/course.reject）写审计；举报处置（report.resolve/report.dismiss）写审计 |
| 权限 | admin 全读写；teacher 仅可提交课程审批与发起举报；student 仅可发起举报；越权 403 |
| 单测 | 全新 **`test_d07_admin.py` 36 用例**（表定义 / schema 校验 / 字段默认值 / 正则校验 / OpenAPI 注册 / 端点计数）· 单测 **215→251 passed** · 0 failed |
| 契约 | 后端端点 **121→141 paths / 158→180 ops** · 模块 **14→16**（新增 admin / settings）· OpenAPI 141 paths |
| 修复 | `@router.post(prefix="")` 不合法（prefix 仅用于 APIRouter 构造器）→ 移除无效参数 |

| 验证 | 结果 |
|------|------|
| `pytest tests` | ✅ 单测 **251 passed** · 0 failed · 13 文件 |
| OpenAPI 检查 | ✅ 141 paths / 180 ops · admin/settings/audit 全部注册 |
| `app.main` 启动 | ✅ 无 import 错误 |

---

### 🎓 D-06 自主学习 + D-08 视频录播（后端 + 移动端 · 2026-09-03）

> WBS-P 自主学习（`lumina-learning-system.html v1.0`）+ 视频录播（D-08）——补齐「学习路径闯关」与「录播回放」两大垂直能力，移动端 5 Tab（学习/视频/成就/历史/我的）全部落地。

| 项 | 内容 |
|----|------|
| 后端 | **13 新表**（44→57）：D-06 `learning_paths`/`learning_path_nodes`/`learning_path_progress`/`user_xp`/`check_in_records`/`badges`/`user_badges`/`challenges`/`challenge_attempts`（9）· D-08 `videos`/`video_chapters`/`video_notes`/`video_watch_history`（4） |
| 模块 | **新建 `learning` 模块 12 端点**（路径列表/地图/加入 · XP 查询 · 打卡/日历 · 徽章列表 · 挑战详情/开始/提交 · 排行榜 · 学习统计）· **新建 `video` 模块 9 端点**（视频列表/详情 · 笔记 CRUD · 章节列表 · 进度上报 · 观看历史/删除）· 合计 +21 端点 |
| 移动端 | **5 新页面**：`Learning.tsx`（学习广场 + 路径地图）· `Videos.tsx`（视频列表 + 播放器 + 笔记）· `Achievements.tsx`（统计 + 打卡日历 + 徽章墙）· `History.tsx`（观看历史）· `Profile.tsx`（个人中心）· API 类型 +19 · `navigation.ts` 注册 5 路由 · `App.tsx` 挂载 5 Screen |
| 单测 | 全新 **`test_d06_d08_learning_video.py` 37 用例**（表定义/schema/OpenAPI）· 单测 **251→288 passed** · 0 failed |
| 契约 | 后端端点 **141→162 paths / 180→201 ops** · 模块 **16→18**（新增 learning / video）· OpenAPI 162 paths |
| 移动端构建 | `tsc --noEmit` **0 错误** · 5 Tab 导航就绪 |

| 验证 | 结果 |
|------|------|
| `pytest tests` | ✅ 单测 **288 passed** · 0 failed · 14 文件 |
| OpenAPI 检查 | ✅ 162 paths / 201 ops · learning/videos 全部注册 |
| `tsc --noEmit` | ✅ 0 错误 · 5 新页面 + 19 类型 + 5 路由 |
| `app.main` 启动 | ✅ 无 import 错误 |

---

### 🤖 D-09 AI 基础设施（后端 · 2026-09-03）

> WBS-P D-09（PRD 15 · P1）——补齐 AI 基础设施三件套：RAG 检索增强生成 / Agent 工具调用 / 内容审核。为后续 RAG 知识库、智能助手、UGC 安全打基础。

| 项 | 内容 |
|----|------|
| 后端 | **5 新表**（57→62）：`knowledge_bases` / `knowledge_chunks`(embedding JSON 存储) / `agent_tools` / `agent_sessions` / `moderation_logs` |
| 模块 | **新建 `ai_infra` 模块 11 端点**：知识库 CRUD · RAG 查询（Python 侧余弦相似度）· Agent 工具列表 · 会话管理 · Agent 对话（启发式工具调用）· 内容审核检查 · 审核日志 |
| RAG 实现 | Mock 嵌入（MD5 哈希 → 16 维向量）· 余弦相似度计算（Python 侧，因 MySQL 9.7 VECTOR 类型无内置距离函数）· top_k 检索 · prompt 增强 · Mock LLM 响应（生产接 ai_gateway） |
| Agent 实现 | 预置 3 工具（get_course_info / search_documents / calculate_grade）· 启发式工具调用（消息含"课程"触发）· 会话消息历史 · 多轮工具调用记录 |
| 内容审核 | 5 敏感词（暴力/赌博/毒品/枪支/诈骗）· 启发式垃圾检测（重复字符）· 返回 flagged/reason/action/confidence · 审核日志自动入库 |
| OpenAPI YAML | 重新生成，与后端同步：**162 paths / 201 ops · 20 tags**（之前 YAML 滞后在 101 paths） |
| 单测 | 全新 **`test_d09_ai_infra.py` 33 用例**（表定义 / 工具函数 / schema / OpenAPI）· 单测 **288→321 passed** · 0 failed |
| 契约 | 后端端点 **162→171 paths / 201→212 ops** · 模块 **18→19**（新增 ai_infra） |

| 验证 | 结果 |
|------|------|
| `pytest tests` | ✅ 单测 **321 passed** · 0 failed · 15 文件 |
| OpenAPI 检查 | ✅ 171 paths / 212 ops · ai-infra 全部注册 |
| OpenAPI YAML | ✅ 同步至 162 paths（与后端一致） |
| `app.main` 启动 | ✅ 无 import 错误 |

### 🔧 D-10 运营演进 · 监控指标（后端 · 2026-09-03）

> WBS-P D-10（运维基线）——补齐运营监控三件套：Prometheus 指标导出 / 深度健康检查 / 业务指标聚合。为生产环境部署、告警配置、运营看板打基础。

| 项 | 内容 |
|----|------|
| 模块 | **新建 `ops` 模块 3 端点**（`/ops/health/deep` 深度健康检查 · `/ops/metrics` Prometheus 文本格式导出 · `/ops/metrics/business` 业务指标聚合（管理员））|
| 深度健康检查 | 检查 MySQL 连通性 + 延迟 · Redis（预留，标记 degraded）· 应用自身 · 返回总体状态（healthy/degraded/down）+ uptime + 版本号 |
| Prometheus | 文本格式（`text/plain; version=0.0.4`）· 7 类指标：`lumina_users_total` / `lumina_courses_total` / `lumina_videos_total` / `lumina_api_requests_total{status}` / `lumina_active_users_today` / `lumina_ai_calls_total` / `lumina_uptime_seconds` |
| 业务指标 | 12 项聚合（total_users / active_users_today / total_courses / active_courses / total_videos / videos_watched_today / total_assignments / submissions_today / total_exams / exam_attempts_today / ai_calls_today / checkins_today）· 仅 admin 可访问 |
| Schema | `ServiceHealth` / `DeepHealthCheckOut` / `PrometheusMetricsOut` / `BusinessMetricsOut` 4 个新 schema |
| 单测 | 全新 **`test_d10_ops.py` 15 用例**（schema 校验 / OpenAPI 注册 / Prometheus 格式规范）· 单测 **321→336 passed** · 0 failed |
| 契约 | 后端端点 **171→174 paths / 212→215 ops** · 模块 **19→20**（新增 ops） |
| OpenAPI YAML | 重新生成，与后端同步：**174 paths / 215 ops · 27 tags** |

| 验证 | 结果 |
|------|------|
| `pytest tests` | ✅ 单测 **336 passed** · 0 failed · 16 文件 |
| OpenAPI 检查 | ✅ 174 paths / 215 ops · ops 3 端点全部注册 |
| OpenAPI YAML | ✅ 同步至 174 paths（与后端一致） |
| `app.main` 启动 | ✅ 无 import 错误 |

### 📟 D-11 鸿蒙原生客户端 · 骨架落地（ArkTS / ArkUI · 2026-09-03）

> WBS D-11（PRD 15 · P2）——补齐鸿蒙原生端，覆盖校园信创场景。选型决策：**原生 ArkTS/ArkUI**（非 RNOH/uni-app），因 Expo 官方不支持鸿蒙，且原生方案可获得分布式通知/平行视界/多窗口/小艺语音等系统能力。

| 项 | 内容 |
|----|------|
| 目录 | **新建 `服务/harmony-app/`**（HarmonyOS NEXT Stage 模型） |
| 项目结构 | `AppScope/` + `entry/src/main/ets/`（entry/pages/common/model 4 子目录）+ 构建配置（hvigorfile / build-profile / oh-package） |
| 6 核心页面 | **Index.ets（登录）** · **Home.ets（5 Tab 主页：首页/课程/直播/学习/我的）** · **LiveRoom.ets（直播观看 + 举手/答题/聊天）** · **Learning.ets（D-06 学习广场 + XP + 打卡 + 徽章墙）** · **Videos.ets（D-08 视频库 + 搜索）** · **Profile.ets（个人中心 + 登出）** |
| 设计系统 | `constants.ets` 完整实现 Lumina 色板（宣纸/墨/钴蓝/荧光黄/警示红/成长绿/AI紫）+ 字号 + 间距 + 圆角 |
| API 客户端 | `ApiClient.ets`（基于 `@kit.NetworkKit` http 模块）· OAuth2 密码登录 · token 管理 · 与 mobile-app `client.ts` 同契约 |
| 类型定义 | 17 个 interface 对齐后端 schema（User/Course/LiveRoom/LearningPath/Video/UserXP/CheckInResult/Badge/Notification 等） |
| 权限声明 | `module.json5` · `ohos.permission.INTERNET` + `ohos.permission.GET_NETWORK_INFO` |
| 文档 | **`harmony-app/README.md`**（开发环境 / 设计系统 / API 契约 / 页面功能 / 鸿蒙特性待接入 / 构建发布） |
| 架构决策 | Expo 不支持鸿蒙 → 原生 ArkTS/ArkUI 方案胜出；RNOH/uni-app 作为备选方案记录在 README |

| 验证 | 结果 |
|------|------|
| 项目结构 | ✅ DevEco Studio Stage 模型标准结构（22 文件） |
| 页面完整性 | ✅ 6 页面覆盖登录/课程/直播/学习/视频/个人中心 |
| API 契约 | ✅ 与后端 174 paths / 215 ops 对接 · 9 个核心端点已封装 |
| 设计一致性 | ✅ Lumina 色板/字号/间距/圆角 完整复用 |
| 真机冒烟 | ⏳ 待华为设备/DevEco 模拟器（本机 Windows 环境无鸿蒙 toolchain） |

| 鸿蒙特性 Phase 2 待接入 | 优先级 |
|----------------------|--------|
| 分布式通知（跨设备流转） | P1 |
| 平行视界（平板双栏） | P1 |
| AVPlayer 真实 HLS 播放 | P1 |
| 多窗口（PC/平板多任务） | P2 |
| Floating Window 视频小窗 | P2 |
| 元服务卡片（桌面 2×2 课表） | P2 |
| 小艺语音（播报提醒） | P3 |
| 车机适配（音频 + D 档安全） | P3 |

### 📄 D-07 文档联动 · QA 测试用例（2026-09-03）

| 文档 | 版本 | 同步内容 |
|------|------|----------|
| `lumina-testcases.html` | v1.5 → **v1.7** | 单测 173→**251**（补 D-05 学情分析 +42 · D-07 管理端 +36）· 测试文件 11→**13** · 统计块（测试模块 13 · 协作+通知+考试+管理单测 100）· 统计口径段 · 测试金字塔 ① 行 · §9.1 覆盖率表新增 tutoring 与 admin（含 settings/audit · `test_d07_admin.py` 36 用例）行 · footer 16 模块 |
| `lumina-00-index.html` | 卡片 | QA 卡 tag v1.5 → **v1.7 · 2026-09-03**（卡内 251 用例 / 16 模块原已预置） |
| `README.md` | 数字 | 文档清单 QA v1.7 · 251 用例 · 版本历史 +QA v1.7 行 |

| 验证 | 结果 |
|------|------|
| 测试目录实测 | ✅ `tests/` 13 个测试文件 · `def test_` 合计 **251**（d05_analytics 42 · d07_admin 36）与文档一致 |
| 冒烟脚本 | `smoke_admin.py` 未创建（D-07 无专项冒烟脚本）· 文档未写入管理端冒烟条目，避免虚构 |
| 残留检查 | ✅ 全文无 v1.5 / 173 旧口径残留 |

---

### 📊 项目全景状态报告（2026-09-03 · 文档整理）

> 整理目前所有需求和规划、开发进度，生成一套全新版本文件记录。

| 项 | 内容 |
|----|------|
| 产出 | 全新 **`原型/lumina-status-report.html`** v1.0 · 10 章节全景报告 |
| 内容 | 里程碑进度（M1-M3 ✅ / M4 ⬜）· 已实现功能清单（D-01~D-05）· 待实现需求规划（D-06~D-10 + M4 上线任务）· 业务模块清单（14 模块 · 121 paths）· 文档体系（12 文档）· 技术栈快照 · 质量指标（215 单测 · 365 req/s）· 风险与阻塞 · 下一步行动 · 版本文件清单 |
| 数据准确性 | 基于 OpenAPI 实际统计：**121 paths / 158 ops · 40 表 · 215 单测 · 14 模块** |
| 联动同步 | 索引页 v1.5→**v1.6**（+状态报告卡片 · 11→12 文档）· README +状态报告条目 · CLAUDE.md +状态报告描述 · MEMORY.md 更新项目状态 |
| 说明 | 提供项目当前状态的"一目了然"视图，便于快速了解整体进展、待办事项和阻塞因素 |

---

### 📝 D-04 题库与考试：题库/试卷/组卷/自动评分 + 主观人工评分/统计（后端 + Web · 2026-09-01）

> WBS-P D-04（PRD 07 · P1）——垂直补齐「题库与考试」闭环：课程题库 CRUD → 试卷编排（时序+时长+分值覆盖）→ 智能组卷 → 学生作答（客观题自动评分）→ 教师主观人工评分 → 统计看板。

| 项 | 内容 |
|----|------|
| 后端 | 全新 **`exam` 模块 22 端点**（`/courses/{id}/questions` CRUD · `/courses/{id}/papers` CRUD + 加题/移除/组卷/发布/关闭 · `/attempts` 作答/评分/统计）· **4 表**（31→35：`exam_questions` / `exam_papers` / `exam_paper_questions`(/`uq_paper_question`) / `exam_attempts`(/`uq_exam_attempt`)）· 自动评分（`scoring.judge_answer` 客观题 set 比较）+ 主观题人工可评 · 单测 **173 passed**（+35，`test_exam.py`） |
| Web | `Exam.tsx` 教师端：题库管理（四题型 CRUD + 难度/标签过滤 + 表单面板）+ 试卷管理（建卷/编排/智能组卷/发布关闭/提交列表 + 主观题人工评分 + 统计正确率条）· `ExamTaking.tsx` 学生端：开始/续答（幂等）/四题型作答/提交 → 客观题即时得分 + 主观题待评提示 · `CourseDetail.tsx` 课程页新增「题库与考试」入口 |
| 冒烟 | `smoke_exam.py` 真库 **28/28**（建题→过滤→建卷→加题→重复 409→组卷→空卷 400→发布→作答→客观题自动评分→主观题未评 null→人工评分→清除评分→统计→越权 403） |
| 契约 | 前端调用 **46→60** · 后端端点 **82→98** · 模块 **12→13** · 三层一致（60⊂98⊂Nginx） |
| 事件目录 | **82→97**，`exam` 命名空间入白名单（exam.* 15 类）· init.sql 重生成 **35 表** |
| 遗留 | 未来可扩展：组卷随机种子可复现增强 / 考试防切屏 / 主观题 AI 辅助评分（候选 D 阶段） |

### 📄 D-04 文档联动 · API/OpenAPI/TDD/DB/QA/OPS/索引（2026-09-01）

| 文档 | 版本 | 同步内容 |
|------|------|----------|
| `lumina-api.html` | v1.5 → **v1.6** | 新增 08 题库与考试章节（22 端点），协作顺延 09 · AI 10 · 章节 12→13 · stats 13 模块 · 98 接口 |
| `lumina-api-openapi.yaml` | 1.5.0 → **1.6.0** | exam 路径 **16 paths / 22 ops** + exam tag + 12 个 exam schemas · 81 paths / 102 ops · 版本与特性描述同步 |
| `lumina-tdd.html` | v1.4 → **v1.5** | 模块 12→13（exam 行 22 端点）· 82→98 · 31→35 · 契约 46⊂82→**60⊂98** |
| `lumina-database.html` | v1.5 → **v1.6** | 新增 08 题库与考试章节 **4 表卡**（含 UNIQUE 约束）· 31→35 · 12→13 模块 · 11→12 章（后章节重编号）· ER/关系说明补 exam |
| `lumina-testcases.html` | v1.4 → **v1.5** | 单测 138→**173**（+35 exam）· 冒烟矩阵补 smoke_exam 28 步 · 统计口径说明 |
| `lumina-operations.html` | v1.4 → **v1.5** | lumina-app 快照 98 端点 · 13 模块 · 35 表 |
| `lumina-00-index.html` | v1.4 → **v1.5** | API v1.6 / TDD v1.5 / DB v1.6 / OPS v1.5 / QA v1.5 / WBS-P v1.3 卡片 · 10 文档 |
| `CLAUDE.md` / `README.md` / `部署/README.md` | 数字 | 快照 98 端点 · 13 模块 · 35 表 · 173 单测 · 60⊂98 契约 · OpenAPI 81 paths |

| 验证 | 结果 |
|------|------|
| 契约核对 `api_contract_check.py` | ✅ 60 前端 ⊂ **98** 后端 ⊂ Nginx，三层一致 |
| `pytest tests` | ✅ 单测 **173 passed** · 0 failed · 11 文件 |
| OpenAPI yaml | ✅ PyYAML 解析通过 · 81 paths · 102 ops · 版本 1.6.0 |

---

### 📄 D-01 文档同步 · API/DB/TDD/WBS-P/索引（2026-08-31）

> 五份文档对齐 D-01 直播落地快照（代码为准）：**60 端点 · 10 模块 · 22 表 · 109 单测**，直播从「规划中 WebSocket」改为「已落地 REST 轮询 + HLS 可插拔」。

| 文档 | 版本 | 同步内容 |
|------|------|----------|
| `lumina-api.html` | v1.2 → **v1.3** | stats 10 模块 · 60 接口；直播章节重写为 **16 REST 端点**（房间/参与/举手/聊天/点名/答题 5 组） |
| `lumina-api-openapi.yaml` | 1.2.0 → **1.3.0** | live 段由 5 条规划（含 WS）→ **15 路径 16 操作** + `/media/{path}` HLS 反代；头部去 WebSocket 特性 |
| `lumina-tdd.html` | v1.1 → **v1.2** | 模块 9→10（新增直播行 16 端点）、表 17→22、合计口径 60（注 api_contract_check 唯一端点）；§7.2 直播行改 REST |
| `lumina-database.html` | v1.2 → **v1.3** | stats 22 表 · 10 模块；直播章节 2 张规划表 → **5 张落地表**（rooms/attendees/messages/quizzes/quiz_answers，含唯一约束） |
| `lumina-wbs-pending.html` | v1.0→**v1.1** | 基线 60/10/22/冒烟 25/25；**D-01 标记 ✅ 已实施**；B-01/B-03/数据迁移/功能验收 17→22、44→60；**v1.1（2026-08-31 晚）：+D-09 鸿蒙适配评估（P2 · 33 任务包）** |
| `lumina-00-index.html` | v1.3 | API/DB 卡 v1.2→**v1.3**、44→60 端点、17→22 表、9→10 模块；TDD 卡 v1.1→v1.2 |
| `CLAUDE.md` / `README.md` / `部署/README.md` / `OPS` / `testcases` / `上线检查清单` / `UAT` | 数字 | 快照 60 端点 · 10 模块 · 22 表 · 109 单测 · 28⊂60⊂28 契约 · 65 事件 |

| 验证 | 结果 |
|------|------|
| 契约核对 `api_contract_check.py` | ✅ 28 前端 ⊂ **60** 后端 ⊂ Nginx，三层一致 |
| `run_tests.py` | ✅ 单测 **109 passed** · 0 failed · 8 文件 |
| OpenAPI yaml | ✅ PyYAML 解析通过 · 43 paths · /media 已含 |

---

### ✅ D-01 收尾 · 回归固化 + 事件目录（2026-08-31）

> 把直播课堂纳入端到端回归冒烟，并把 `live.*` 埋点写进事件目录，保护已交付功能（含真实 HLS 流）在后续迭代不回归。

| 项 | 内容 |
|----|------|
| smoke_test.py | 新增 **S16-S24 直播用例**（10 步）：建房间→开播(stream_url 断言)→学生入会→**未选课越权 403**（LUMINA_NOUSER_* 可选 env）→举手队列→随机点名/应答→聊天/after_id 增量→答题闭环(发布/作答/关闭/统计)→结束 |
| 关系 | `部署/scripts/events_catalog.py` 命名空间白名单 `+live`；重新生成 `events-catalog.md`（**65 事件**，13 个 live.* 已入目录）· `--check` 命名规范合规 |
| UAT | `UAT-验收清单.md` 新增 **6. 直播课堂** 6 项验收 + 埋点/跨端章节重编号（7/8） |
| 验证 | 在线环境全量冒烟 **25 PASS / 0 FAIL / 1 SKIP**（AI 批阅未启用）：既有 S1-S15 无回归 + 直播 S16-S24 全绿 |

---

| 里程碑 | 定义 | 状态 |
|--------|------|------|
| M1 | 基础环境可用（compose 一键启动 4 基础服务） | ✅ W2 达成 |
| M2 | 核心功能开发完成（单体 FastAPI 58 端点） | ✅ 达成 |
| M3 | 测试通过（全部用例 + 埋点验证） | ✅ 2026-08-31 达成 |
| M4 | 正式上线（数据迁移 + 灰度 + 全量） | ⬜ W10 |

---

## 开发环境备注

- 本机仅 Python 3.14.4 → 本地验证用 `.venv` 新版本包；生产锁定版本跑在 Docker python:3.12
- MySQL 未起时，集成测试自动跳过（skip），不阻塞 CI
- Windows GBK 编码：运行 `pytest` 前设置 `PYTHONIOENCODING=utf-8`

---

## 📌 技术变更记录（2026-08-31）

### 数据库更换：PostgreSQL 16 → MySQL 9.7

**变更原因**：按变更工作流对齐生产线技术栈，MySQL 生态成熟、运维简单、部署成本低，与 `utf8mb4` 字符集和现有 JSON 需求匹配。

**变更范围**：

| 层 | 文件 | 变更 |
|----|------|------|
| 代码 | `服务/lumina-app/app/models.py` | 新增 `GUID` TypeDecorator（CHAR(36)），38 处 `UUID(as_uuid=True)` 替换；原生 text-SQL 绑参兼容 |
| 代码 | `服务/lumina-app/app/config.py` + 8 个模块 config | `DATABASE_URL` 默认值 → `mysql+pymysql://...` |
| 代码 | `服务/lumina-app/app/modules/ai_gateway/main.py` | `migrate_schema()` 改为 `inspect` 探测 + MySQL 兼容 ALTER |
| 代码 | `服务/lumina-app/requirements.txt` | 新增 `PyMySQL==1.1.1` |
| 部署 | `部署/docker-compose.yml` | `postgres` 服务 → `mysql:9.7`（`lumina_mysql` · utf8mb4 · healthcheck） |
| 部署 | `部署/.env.example` | `POSTGRES_*` 段 → `MYSQL_*` 段 |
| 部署 | `部署/config/mysql/init.sql` | 用 SQLAlchemy MySQL 方言编译生成（17 表 · 38 CHAR(36)） |
| 部署 | `部署/scripts/*` | `start/stop/logs/backup/monitor/status/seed_demo` 适配 MySQL（mysqldump / information_schema.sql / sql 恢复） |
| 文档 | `原型/lumina-database.html` v1.1 → v1.2 | PostgreSQL → MySQL 9.7 · 单库模型 · 17 表 · 分区/迁移章节同步 |
| 文档 | `原型/lumina-tdd.html` `lumina-operations.html` `lumina-testcases.html` `lumina-prd.html` `lumina-launch-wbs.html` `lumina-00-index.html` | 数据层标识同步 MySQL 9.7 |
| 文档 | `README.md` `CLAUDE.md` `部署/README.md` `部署/docs/上线检查清单.md` `部署/docs/UAT-验收清单.md` | PostgreSQL/ pg_data / 5432 / psql 表述 → MySQL / mysql_data / 3306 / mysql |

**验证结果**（既有 `服务/lumina-app/.venv`，2026-08-31）：
- ✅ `app.openapi()` 生成成功，`paths = 46`
- ✅ `pytest` 77 passed · 4 skipped（bcrypt 兼容 skip）
- ✅ 契约核对 `api_contract_check.py` 三层一致（13 前端 URL ⊂ 44 端点 ⊂ Nginx）
- ✅ `docker-compose.yml` 解析通过，services = `['mysql', 'redis', 'lumina-app', 'nginx']`

**遗留**：历史 WBS 进度行（阶段一/二表内）保留当时 PostgreSQL 表述，仅当前待办与文档已更新；阶段三/四真实执行待 MySQL + Docker 环境就绪。